<pre>

● 1. DB 유저 + 스키마
  
데이터베이스 유저 계정
    고유 유저명
    인증 방식
    기본 테이블 스페이스
    임시 테이블 스페이스
    유저 프로파일
    초기 Consumer Group
    계정 상태

스키마 : 유저 계정과 동일한 이름
    데이터베이스 객체 모음 (유저 소유)



① Username : 사용자 아이디
  
② 인증 방식 : 어떻게 로그인 할지
    Password      : 아이디 + 비밀번호
    OS 인증       : OS 계정
    External 인증 : 다른 인증 시스템

③ 기본 테이블 스페이스 : 테이블을 만들 때 기본적으로 저장되는 공간
  
④ 임시 테이블 스페이스 : SQL 실행 중에 정렬ㆍ임시 작업 공간  ( ORDER BY,  GROUP BY,  HASH JOIN )

⑤ 유저 프로파일
    비밀번호 만료기간
    로그인 실패 횟수 제한    ex) 5번 로그인 실패 시 계정 잠금
    세션 시간 제한

⑥ 초기 Consumer Group : 그룹별 시스템 자원을 얼마나 쓸 지 정함 
    개발자 그룹
    관리자 그룹
    사용자 그룹

⑦ 계정 상태
    OPEN    → 사용 가능
    LOCKED  → 잠김
    EXPIRED → 비밀번호 만료

⑧ 스키마 : 사용자가 만든 객체들의 집합 ( 테이블, 인덱스, 뷰, 프로시저 ... )  scott 스키마라고 부르는 이유
                      
  

create user king                    -- 아이디
identified by tiger                 -- 인증방식
   default tablespace data_ts02     -- 기본 테이블 스페이스
 temporary tablespace temp          -- 임시 테이블 스페이스
   profile default                  -- 프로파일
   account lock;                    -- 계정 상태

alter user king account unlock;  -- lock 풀어주기




-- OS 인증 방법
  
show parameter authent
NAME                                 TYPE        VALUE
------------------------------------ ----------- ------------------------------
os_authent_prefix                    string      ops$

create user ops$oracle identified externally;   -- externally
grant connect to ops$oracle;

$ sqlplus /      -- OS 인증

show user
USER is "OPS$ORACLE"


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 2. 프로파일 지정 1 - Resource


  
- 유저에게는 한번에 하나의 프로파일만 할당 (반드시 1개의 프로파일 가짐)

USER_A → PROFILE_A
USER_B → PROFILE_LIMIT


- 역할 : 사용자가 DB 자원을 얼마나 사용할 수 있는지 제한

① 리소스 사용 제한
   CPU 사용량
   세션 시간          
   동시 접속 수
   읽기 블록 수
   CPU/Session          : 한 세션이 사용할 수 있는 CPU 시간 제한
   Idle Time            : 사용자가 아무 작업 안 하는 최대 시간       Idle Time = 60  (60분)
   Concurrent Sessions  : 한 사용자가 동시에 접속할 수 있는 세션 개수
   Connect Time         : 한 번 접속했을 때 최대 사용 시간           120 : 2시간 후 자동 종료

② 계정 상태 및 비밀번호 관리
    비밀번호 만료기간
    로그인 실패 횟수 제한    ex) 5번 로그인 실패 시 계정 잠금
    세션 시간 제한

  
※ 리소스 제한 적용 
    ALTER SYSTEM SET RESOURCE_LIMIT=TRUE;  -- 이걸해야 리소스 제한이 먹힘
  
    CPU 제한
    세션 제한
    Idle Time



  
- idle time 제한

create profile limited_user_profile
limit 
cpu_per_session 1000   -- cpu 사용시간 : 1000시간
idle_time 5            -- 5분
sessions_per_user 2;   -- 세션 수 : 2 

alter user king profile limited_user_profile; -- 적용 (5분 동안 놀면 접속 끊김)
 
select username, profile from dba_users; -- 확인



- 블록 제한

alter  profile  LIMITED_USER_PROFILE 
limit
LOGICAL_READS_PER_CALL 100;

select count(*) from emp, emp, emp, emp, emp, emp, emp, emp, emp, emp;
ERROR at line 2: ORA-02392: exceeded session limit on CPU usage, you are being logged off




- 제한 풀기

alter  profile  LIMITED_USER_PROFILE 
limit
LOGICAL_READS_PER_CALL unlimited;
 
alter  profile LIMITED_USER_PROFILE
limit 
SESSIONS_PER_USER  unlimited
CPU_PER_SESSION unlimited
IDLE_TIME  unlimited;


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 3. 유저 프로파일 지정 2 - 비밀번호

비밀번호 만료 관리     : PASSWORD_LIFE_TIME 90  ( 90일 후 비밀번호 만료 )
                      : PASSWORD_GRACE_TIME 7  ( 7일 안에 비밀번호 변경, 경고 뜸 )
                      : PASSWORD_LOCK_TIME 1   ( 계정이 잠기면 1일 후 자동 해제 )
로그인 실패 제한       : FAILED_LOGIN_ATTEMPTS 5  ( 5번 틀리면 Lock )
비밀번호 재사용 방지   : PASSWORD_REUSE_TIME 30  ( 30일 동안 같은 비밀번호 사용 불가 )
                      : PASSWORD_REUSE_MAX 5  ( 비밀번호 5번 변경 시 이전 비밀번호 사용 가능 )
비밀번호 복잡도 검사  : PASSWORD_VERIFY_FUNCTION VERIFY_FUNCTION_11G
   최소 길이
   숫자 포함
   특수문자 포함
   사용자 이름 포함 금지



CREATE PROFILE pwd_test_profile LIMIT
  PASSWORD_LIFE_TIME       1
  PASSWORD_GRACE_TIME      1
  FAILED_LOGIN_ATTEMPTS    3
  PASSWORD_LOCK_TIME       1/1440
  PASSWORD_REUSE_TIME      30
  PASSWORD_REUSE_MAX       3
  PASSWORD_VERIFY_FUNCTION ora12c_verify_function;

alter user king profile pwd_test_profile;   -- 유저에게 적용


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 4. 유저에게 공간 할당량 지정 (Quota)

옵션                설명
특정 값 (MB / KB)   해당 크기까지만 사용 가능 (예: 20MB)
UNLIMITED           테이블스페이스 공간 전체를 제한 없이 사용 가능
NONE (0)            객체 생성 불가 (기본값)

ALTER USER scott QUOTA 0 ON users;           -- 할당량 회수 (0으로 설정)
ALTER USER scott QUOTA 20M ON users;         -- 특정 크기 부여
ALTER USER scott QUOTA UNLIMITED ON users;   -- 무제한 부여


create user jack identified by tiger;
grant connect to jack;          -- 세션 접속
grant create table to jack;     -- 테이블 생성 권한  (INSERT 권한 없)

-- jack
insert into emp90 values(1111,'scott', 3000);
ERROR at line 1: ORA-01950: no privileges on tablespace 'USERS'

-- jack에게 권한 주기
alter user jack quota 10m on users;  -- 테이블 스페이스임


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 5. DB 관리자 권한 인증 : SYSDBA, SYSOPER, SYSASM 접속 검증


- 일반 유저 생성 (SYSDBA 처럼 접속)
  
# useradd oracle2
# id oracle2
uid=1001(oracle2) gid=1001(oracle2) groups=1001(oracle2)
# su - oracle2
$ sqlplus / as sysdba
bash: sqlplus: 명령을 찾을 수 없습니다...


- DBA 권한 주기
  
# usermod -aG dba oracle2
# id oracle2
uid=1001(oracle2) gid=1001(oracle2) groups=1001(oracle2),54322(dba)
# su - oracle2
$ sqlplus / as sysdba  -- 접속 성공



- 관리자 인증 방식 (우선 순위 : OS 인증 > Password File 인증)

① OS 인증            : sqlplus / as sysdba
② Password File 인증 : sqlplus sys/oracle@orcl as sysdba

$ sqlplus sys/oracle_4U@192.168.13.181:1521/orcl as sysdba  -- sys 유저로 다른 툴 (SQL Developer)에서 접속 가능 



- 비밀번호 파일 삭제
$ cd $ORACLE_HOME/dbs
$ rm orapw[ORACLE_SID]
$ sqlplus sys/oracle_4U@192.168.13.181:1521/orcl as sysdba  -- 로그인 실패

- 비밀번호 파일 생성
$ orapwd file=orapw[ORACLE_SID] password=[PASSWORD] format=12 force=y
$ sqlplus sys/oracle_4U@192.168.13.181:1521/orcl as sysdba  -- 로그인 성공





- Password File 인증 유저 확인  ( 파일 탈취시 원격 sysdba 접속 가능  →  해킹 가능해짐 (쿠팡처럼 됨) )

<img width="1524" height="139" alt="image" src="https://github.com/user-attachments/assets/df817261-da2b-45f8-84f0-e90e97a2b993" />


- 감사 파일 위치 확인  ( sysdba, sysoper 접속은 audit_file_dest의 .aud 파일에 자동 기록)

<img width="671" height="180" alt="image" src="https://github.com/user-attachments/assets/4da728e5-2fa9-4397-90ac-c0cefe64acd3" />



  
  
- 불필요한 유저 빼야함
  
# vi /etc/group
# grep dba /etc/group
dba:x:54322:oracle
backupdba:x:54324:oracle
dgdba:x:54325:oracle
kmdba:x:54326:oracle
racdba:x:54330:oracle



- 외부 접속 불가하게 만들기
  
alter system set remote_login_passwordfile='none' scope=spfile;
  
  
show parameter remote
NAME                                 TYPE        VALUE
------------------------------------ ----------- ------------------------------
remote_login_passwordfile            string                                      -- 이거 EXCLUSIVE면 외부 접속 가능
  
</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 6. 최소 권한의 원칙


① 데이터 딕셔너리
  
 - 유저에게 업무수행에 필요한 최소한의 권한만 부여하는 보안 원칙
 - 과도한 권한은 실수ㆍ해킹 공격 등의 보안 피해 생길 수 있음

O7_DICTIONARY_ACCESSIBILITY = FALSE  -- 기본값 (권장) : SYSDBA 권한 없이 딕셔너리 객체 접근 불가

SHOW PARAMETER O7_DICTIONARY_ACCESSIBILITY;

ex) smith 일반 개발자 계정

-- false
select * from dba_tables;  
ERROR at line 2: ORA-00942: table or view does not exist


-- true
alter system set O7_DICTIONARY_ACCESSIBILITY=true scope=spfile;
shutdown immediate
startup

select count(*) from dba_tables;
  COUNT(*)
----------
      2785

  
  
② Public 권한 ( 모든 유저가 가진 권한  →  최소화 필수 )

SELECT OWNER, TABLE_NAME, PRIVILEGE
  FROM DBA_TAB_PRIVS
 WHERE GRANTEE = 'PUBLIC';

REVOKE EXECUTE ON UTL_FILE FROM PUBLIC;    -- 불필요 권한 회수
REVOKE EXECUTE ON UTL_HTTP FROM PUBLIC;
REVOKE EXECUTE ON DBMS_ADVISOR FROM PUBLIC;



③ 디렉토리 접근 제한

-- 특정 유저에게만 디렉토리 권한 부여
GRANT READ, WRITE ON DIRECTORY data_dir TO scott;

-- 불필요한 권한 회수
REVOKE READ ON DIRECTORY data_dir FROM PUBLIC;

  

④ 관리 권한 최소화 (DBA)
  
SELECT GRANTEE     -- DBA 권한
  FROM DBA_ROLE_PRIVS
 WHERE GRANTED_ROLE = 'DBA';

GRANT CREATE SESSION, CREATE TABLE TO [DEV_USER];
GRANT DBA TO [DEV_USER];  -- X : 필요한 권한만 개별 부여



⑤ 원격 OS 인증 차단 : TRUE일 경우, OS 계정이름만으로 DB 접속 가능

REMOTE_OS_AUTHENT = FALSE;  -- 변경 시 재시작 필요
SHOW PARAMETER REMOTE_OS_AUTHENT;

ex)

-- false
$ sqlplus /@[서버 IP]:[포트]/[서비스명]

ERROR: ORA-01017: ....


-- true
alter system set remote_os_authent=true scope=spfile;
shutdown immediate
startup

$ sqlplus /@[서버 IP]:[포트]/[서비스명]

-- 접속 성공





항목             핵심 조치
딕셔너리 보호    O7_DICTIONARY_ACCESSIBILITY =FALSE
PUBLIC 권한      불필요한 패키지 권한 회수
디렉토리         필요한 유저에게만 READ/WRITE 부여
관리 권한        DBA Role 최소화, 개별 권한 부여 원칙
원격 OS 인증     REMOTE_OS_AUTHENT = FALSE 유지



</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 7. WITH ADMIN OPTION : 시스템 권한을 부여받은 유저가 해당 권한을 다른 유저에게 재부여

ex) create session (DBA가 HR에게 CREATE SESSION을 준 상황)

DBA → HR → SH (권한 부여 가능)
         ← SH (권한 회수 가능)
DBA ← HR (본인 권한 반납 가능) : 반납해도 SH의 권한은 살아있음



</pre>
