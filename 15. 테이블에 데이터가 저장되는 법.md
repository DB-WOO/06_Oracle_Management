<pre>

  
● 1. row migration 현상

- 이미 데이터 블록에 저장된 데이터의 길이가 UPDATE로 길어질때 발생
  PCTFREE이 부족하여 수정된 데이터를 수용할 수 없으면, 해당 ROW 전체를 새로운 블록으로 옮김

  
update emp set ename = 'bbb' where ename = 'aaa';

→ aaa를 bbb로 변경 : aaa 블럭을 bbb로 수정하는 게 아님
                     bbb 블럭을 새로 구성하고, aaa 블럭은 어느 블럭으로 이사갔다고 기록됨


- PCTFREE 영역 : 데이터 블록 내에서 UPDATE 작업으로 인한 행 길이 증가에 대비해 남겨두는 예비 공간
                 row migration 현상 예방 (현상 일어나면 select 느려짐)



  
# pctfree 사이즈 조정

alter table emp pctfree 40;

select table_name, pct_free from user_tables;
TABLE_NAME   PCT_FREE
---------- ----------
EMP                40
DEPT               10
EMP07              10
EMP22              10



# pctfree 자동 조정 테이블 스페이스 만들기
  
  create tablespace ts40
datafile '[HOME]/oradata/ts40.dbf' size 10m
 segment space management auto;





# row migration 현상 테스트

@?/rdbms/admin/utlchain.sql
( 오라클 데이터베이스에서 Row ChainingㆍRow Migration 현상이 발생한 데이터를 분석하기 위해 
필요한 CHAINED_ROWS 테이블을 생성하는 스크립트 )


  
-- emp 테이블에 row migration 현상이 일어났는지 분석

analyze table emp list chained rows;
select count(*) from chained_rows;  -- 0 : row migration 발생 안함

  

-- row migration 유발

alter table emp pctfree 1;
alter table emp modify job varchar2(4000);

update emp set job='aaaaaa ... aaaaaaa';   -- 엄청 길게

analyze table emp list chained rows;
select count(*) from chained_rows; -- 2 : emp 검색 시 성능 떨어지게 됨



-- 위 현상 해결

select table_name, tablespace_name from user_tables where table_name='EMP';  -- USERS
alter table emp move tablespace ts10;   -- 다른 테이블 스페이스로 move

truncate table chained_rows;            -- emp 테이블을 분석하고 row migration된 row들이 정리되었는지 확인
analyze table emp list chained rows;    -- 분석
select count(*) from chained_rows;      -- 0


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>


  
● 2. row chaining 현상

- 한 ROW의 크기가 처음부터 데이터 블록 1개보다 클때 발생
  컬럼의 개수 255개 초과하여 하나의 블록에 담기지 못할 때 나타남


구분         Row Migration (행 이주)                      Row Chaining (행 연결)
발생 시점    주로 UPDATE 시 발생                          주로 INSERT 시 발생
주요 원인    가변 길이 컬럼 수정 및 공간 부족              데이터 크기가 블록 크기보다 큼
물리적 구조  원래 위치엔 포인터만, 실제 데이터는 다른 곳   데이터가 여러 블록에 나뉘어 연결됨
해결 방법    PCTFREE 설정값 상향, Reorg 작업              블록 크기 확대, 테이블 설계 최적화


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>


  
● 3. DB reorg 작업 : 테이블이나 인덱스의 물리적인 저장 구조를 다시 정리하여 데이터 접근 효율을 높이는 작업


# Fragmentation (단편화) 해결

데이터가 삭제된 공간이 중간중간 생기지만 새로운 데이터가 그 크기에 딱 맞지 않아 활용되지 못하는 현상
Reorg로 흩어진 빈 공간을 모아 연속된 가용 공간으로 만듬


# Row MigrationㆍChaining 해소

하나의 로우를 읽을 때 여러 블록을 뒤져야 하는 비효율을 제거
Reorg를 하면 데이터가 인접한 블록에 모임


# High Water Mark 하향 조정

오라클에서 데이터를 읽을 때 "여기까지 데이터가 들어온 적이 있다"라고 표시하는 지점
데이터를 많이 지워도 HWM은 자동으로 내려가지 않아 Full Table Scan 시 데이터가 없는 빈 블록까지 다 읽게 됨
Reorg는 HWM를 실제 데이터 끝으로 재설정


# 인덱스 밸런싱

인덱스 엔트리가 한쪽으로 치우치거나(Skewed) 삭제된 리프 블록이 재사용되지 않는 경우 인덱스 높이가 높아짐
Reorg(Index Rebuild)를 통해 인덱스 트리를 최적화



# Reorg 해보기 ( 테이블 move )

insert into emp select * from emp;
/  ← 10번
ERROR at line 1: ORA-01653: unable to extend table SCOTT.EMP by 128 in tablespace TS10

commit; 

delete  from  emp  where  deptno in (10,20);   -- 빈 공간 많이 생김
commit; 


-- emp HWM 확인
select count(distinct dbms_rowid.rowid_block_number(rowid) ) blocks from emp;  -- 1192



-- 테이블 스페이스 조회ㆍ생성ㆍ이동

select table_name, tablespace_name from user_tables where table_name='EMP';  -- ts10

create tablespace ts101 datafile '/home/oracle/oradata/ts101.dbf' size 50m;

alter table emp move tablespace ts101;

select count(distinct dbms_rowid.rowid_block_number(rowid) ) blocks from emp; -- 512 : HWM 내려감


  
</pre>
