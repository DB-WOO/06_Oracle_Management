<pre>

● 1. 리소스 매니저 구현

 - CPU, 메모리, I/O 등의 자원을 사용자 그룹별로 할당ㆍ제어
 - 리소스 양 : OLTP  >  DSS  >  Batch

 장점 : 자원 독점 방지 (특정 세션/배치)
        우선 순위 보장 (OLTP)
        혼합 부하 최적화 (OLTP, DSS, Batch 공존 환경)
        DB 레벨 제어
        자동 그룹 전환 : 장시간 실행 쿼리를 낮은 우선 순위 그룹으로 이동

 단점 : 설정 복잡 Plan, Consumer Group, Directive 구성 필요
        잘못 설정 시 (우선순위/배분 등) 성능 저하 유발
        Enterprise Edition 전용 : Standard Edition 사용 불가
        동적 변화 대응 한계     : 부하가 자주 바뀌면 플랜 수정 필요


Resource Plan           : 리소스 할당에 대한 큰 계획         ( 낮, 밤 )
Resource Consumer Group : 리소스 요구사항이 유사한 세션 그룹 ( OLTP, Batch )
Resource Plan Directive : 각 Consumer Group에 리소스를 어떻게 분배할지 정하는 규칙


-- 구현 흐름
Pending Area 생성 → Consumer Group 생성 → Resource Plan 생성
  → Directive 설정 → 검증 → 적용(Submit) → 사용자 연결



  
-- Pending Area 생성 ( system 유저 (sysdba 아님) )
conn system/oracle_4U
exec dbms_resource_manager.create_pending_area();
  


  
-- Consumer Group 생성

SYS_GROUP : DBA,  OTHER_GROUPS : 리소스 제한 높은 유저   →   이미 존재
ONLINE_USERS : 직접 생성 / 온라인 업무 유저

BEGIN
dbms_resource_manager.create_consumer_group( 'ONLINE_USERS'
                                           , 'Users for Online' );
END;
/

SELECT consumer_group    -- 생성 확인
  FROM dba_rsrc_consumer_groups
 WHERE consumer_group LIKE '%ONLINE%';



  
-- Resource Plan 생성

BEGIN
    dbms_resource_manager.create_plan( 'DAYTIME'          -- 플랜명
                                     , 'Plan for day' );  -- 설명
END;
/

SELECT plan FROM dba_rsrc_plans;  -- 생성 확인




-- Plan Directive 생성 (자원 분배 지시어)
  
① 1순위 : DBA
BEGIN
    dbms_resource_manager.create_plan_directive(
        'DAYTIME',
        'SYS_GROUP',
        '1st Group Set',
        cpu_p1 => 100  );
END;
/

② ONLINE_USERS — CPU 2순위 70%, Lock 대기 30초 제한
BEGIN
    dbms_resource_manager.create_plan_directive(
        'DAYTIME',
        'ONLINE_USERS',
        '2nd Group Set',
        cpu_p2                => 70,
        max_idle_blocker_time => 30
    );
END;
/

③ OTHER_GROUPS — CPU 2순위 30%, 병렬도 4 제한
BEGIN
    dbms_resource_manager.create_plan_directive(
        'DAYTIME',
        'OTHER_GROUPS',
        '3rd Group',
        cpu_p2                   => 30,
        parallel_degree_limit_p1 => 4      -- /*+ parallel(emp, 8) */  이걸로 조회 안되는 듯
    );
END;
/




-- 검증 및 적용

exec dbms_resource_manager.validate_pending_area();  -- 검증
exec dbms_resource_manager.submit_pending_area();    -- 실제 DB 반영




-- 결과 조회

SELECT plan
     , group_or_subplan
     , cpu_p1
     , cpu_p2
     , parallel_degree_limit_p1  AS dop
     , max_idle_blocker_time     AS block_t
  FROM dba_rsrc_plan_directives
 WHERE plan = 'DAYTIME'
 ORDER BY 1, 2;

PLAN     GROUP         CPU_P1   CPU_P2    DOP   BLOCK_T
DAYTIME  OTHER_GROUPS  -        30        4     -
DAYTIME  ONLINE_USERS  -        70        -     30
DAYTIME  SYS_GROUP     100      -         -     -




-- 사용자 그룹 전환
begin
 dbms_resource_manager_privs.grant_switch_consumer_group(
    'SCOTT',
    'ONLINE_USERS',
    true
);
end;
/


-- 기본 Consumer Group
begin
 dbms_resource_manager.set_initial_consumer_group(
    'SCOTT',
    'ONLINE_USERS'
);
end;
/


-- 적용 확인
SELECT username
     , initial_rsrc_consumer_group
  FROM dba_users
 WHERE username = 'SCOTT';




-- DAYTIME (방금 생성한 플랜) 활성화
ALTER SYSTEM SET resource_manager_plan = DAYTIME;


-- SCOTT 2개 터미널 로우 락 실행
UPDATE emp SET sal = 0    WHERE ename = 'ALLEN';  -- 로우 블로커 (1번 세션)
UPDATE emp SET sal = 6000 WHERE ename = 'ALLEN';  -- 로우 웨이터 (2번 세션)

→ 아까 ONLINE 유저 30초 대기제한 해놔서 1번 세션은 Kill되고, 2번 세션 Update 완료


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 2. UNDO 사용량 제한

- UNDO : 트랜잭션 롤백, 읽기 일관성, FlashBack 기능을 위해 필요한 핵심 자원
         얘도 한 세션이 점유하면, 같은 DB내 다른 작업들이 영향을 받음

  성능을 좋게 하는게 아니라, 시스템을 보호하는 장치


-- Resource Plan 지정
alter system set resource_manager_plan='daytime';
  
-- Pending Area 생성
exec dbms_resource_manager.create_pending_area();

-- Undo 제한 설정
begin
    dbms_resource_manager.update_plan_directive(
      plan             => 'DAYTIME',
      group_or_subplan  => 'ONLINE_USERS',
      new_undo_pool     => 10);   -- 10KB 제한, NULL로 설정 시 UNLIMITED
end;
/

-- 실제 적용
exec dbms_resource_manager.submit_pending_area();

-- 설정 확인
SELECT plan, group_or_subplan, undo_pool
  FROM dba_rsrc_plan_directives
 WHERE plan = 'DAYTIME';



-- 대량 DML (장애 유발)

create table emp2
    as
select * from hr.employees;

ORA-30027: Undo quota violation ...


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 3. 악성 SQL 제한

-- 현재 설정
SELECT plan, group_or_subplan, cpu_p1, cpu_p2
  FROM dba_rsrc_plan_directives
 WHERE plan = 'DAYTIME';

-- Pending Area 생성
exec dbms_resource_manager.create_pending_area();

-- CPU 제한 설정 (cpu_p2 = 1%)
begin
    dbms_resource_manager.update_plan_directive(
      plan             => 'DAYTIME',
      group_or_subplan => 'ONLINE_USERS',
      new_cpu_p2       => 1);   -- p1(SYS_GROUP)이 사용하고 남은 CPU 중 1% 할당
end;
/

-- Pending Area 제출
exec dbms_resource_manager.submit_pending_area();



-- 생성

-- 예상 실행시간 10초 초과 시 차단  
begin
    dbms_resource_manager.update_plan_directive(
      plan                  => 'DAYTIME',
      group_or_subplan      => 'ONLINE_USERS',
      new_max_est_exec_time => 10);   -- 예상 실행시간 10초 초과 시 차단
end;
/
  
-- 제출

-- 악성 쿼리 ( scott )
select count(*) from emp, emp, emp, emp, emp, emp;
ORA-07455: estimated execution time ...



  

-- 생성
-- 제한해제
begin
    dbms_resource_manager.update_plan_directive(
      plan                  => 'DAYTIME',
      group_or_subplan      => 'ONLINE_USERS',
      new_max_est_exec_time => 0);   -- 0 = UNLIMITED (제한 해제)
end;
/
-- 제출

SELECT plan
     , group_or_subplan
     , max_est_exec_time
  FROM dba_rsrc_plan_directives
 WHERE plan             = 'DAYTIME'
   AND group_or_subplan = 'ONLINE_USERS';

PLAN      GROUP_OR_SUBPLAN   MAX_EST_EXEC_TIME
--------- ------------------ ------------------
DAYTIME   ONLINE_USERS       (null)



-- 리소스 제한 해제
alter system set resource_manager_plan='';

</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 4. 병렬도 제한

  
- 한 세션이 혼자 아주 크게 병렬도를 쓸 시 지만 빠르고, 남들은 작업이 안됨

-- 병렬 쿼리 유도 ( scott )
select /*+ parallel(s1,4) parallel(s2,4) */ count(*)
  from sh.sales s1, sh.sales s2;

-- 병렬 프로세스 확인 ( sys )
select process, program
  from v$session
 where program like '%(P0%';
  

-- 병렬도 ( DOP ) 1로 제한 ( system (sys 아님) )
exec dbms_resource_manager.create_pending_area();  -- 생성
begin
  dbms_resource_manager.update_plan_directive(
    plan                         => 'DAYTIME',
    group_or_subplan             => 'ONLINE_USERS',
    new_parallel_degree_limit_p1 => 1
  );
end;
/
exec dbms_resource_manager.submit_pending_area();  -- 제출


-- 설정 반영 확인
select plan, group_or_subplan, cpu_p1, cpu_p2,
           parallel_degree_limit_p1 dop, max_idle_blocker_time block_t,
           max_idle_time, undo_pool
  from dba_rsrc_plan_directives
 where plan = 'DAYTIME'
 order by 1, 2;


  
-- 병렬도 크게 실행 후 1로 제한되는 지 확인  ( 위에 병렬 프로세스 확인 쿼리 )
  
select /*+ parallel(s1, 8) parallel(s2, 8) */ count(*)
  from sh.sales s1, sh.sales s2;

select process, program
  from v$session
 where program like '%(P0%';


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 5. 낮 / 밤 플랜

낮 - OLTP
밤 - Batch, 갱신 등..


-- 생성 ( system )
exec dbms_resource_manager.create_pending_area();


-- 컨슈머 그룹 생성
exec dbms_resource_manager.create_consumer_group('BATCH_USERS','Users for Batch');

-- 생성 확인
select consumer_group
  from dba_rsrc_consumer_groups
 where consumer_group like '%BATCH%';

-- 밤 플랜 생성
begin
  dbms_resource_manager.create_plan('NIGHTTIME','Plan for night');
end;
/

select plan from dba_rsrc_plans;  -- 생성 확인



-- 분배 정책

-- SYS_GROUP: CPU 1순위 100%
begin
       dbms_resource_manager.create_plan_directive(
         'NIGHTTIME',
         'SYS_GROUP',
         '1st Group Set',
         cpu_p1 => 100
       );
end;
/

-- BATCH_USERS: CPU 2순위 70%, idle blocker time 30초
begin
       dbms_resource_manager.create_plan_directive(
         'NIGHTTIME',
         'BATCH_USERS',
         '2nd Group set',
         cpu_p2 => 70,
         max_idle_blocker_time => 30
       );
end;
/

-- OTHER_GROUPS: CPU 2순위 30%, 병렬도 제한 4
begin
       dbms_resource_manager.create_plan_directive(
         'NIGHTTIME',
         'OTHER_GROUPS',
         '3rd Group',
         cpu_p2 => 30,
         parallel_degree_limit_p1 => 4
       );
end;
/


-- 검증
exec dbms_resource_manager.validate_pending_area();

-- 구현(적용)
exec dbms_resource_manager.submit_pending_area();


-- 설정 조회
select plan
     , group_or_subplan
     , cpu_p1
     , cpu_p2
     , parallel_degree_limit_p1 dop
     , max_idle_blocker_time block_t
  from dba_rsrc_plan_directives
 where plan = 'NIGHTTIME'
 order by 1,2;


  

-- SCOTT 사용자를 BATCH_USERS로 지정 (권한 + 기본 그룹)
begin
dbms_resource_manager_privs.grant_switch_consumer_group(
       'SCOTT', 'BATCH_USERS', true                -- 컨슈머 그룹 전환 권한 부여 (with grant option = true)
     );
end;
/

begin
 dbms_resource_manager.set_initial_consumer_group(
       'SCOTT', 'BATCH_USERS'                     -- 기본(초기) 컨슈머 그룹 지정
     );
 end;
 /

select username, initial_rsrc_consumer_group  -- 확인
  from dba_users
 where username = 'SCOTT';



-- 야간 플랜 적용
alter system set resource_manager_plan='nighttime';

-- 파라미터 확인
show parameter resource

-- 주간 플랜 적용 (DBA 출근 시)
alter system set resource_manager_plan='daytime';

-- 야간 플랜 적용 (DBA 퇴근 시)
alter system set resource_manager_plan='nighttime';


</pre>
