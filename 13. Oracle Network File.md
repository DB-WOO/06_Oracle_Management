<pre>


listener.ora : 서버 아이피 주소, 포트, 서비스 이름, 프로토콜 정보 (서버)
sqlnet.ora   : 접속 허용/거부 아이피 설정
tnsnames.ora : 여러 서버들에 접속하기 위한 오라클 접속정보 저장   (클라이언트)



● 1. 서버 listener.ora 파일 내용 확인

$ cd $ORACLE_HOME/network/admin
$ cat listener.ora

LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST = [서버 IP])(PORT = [PORT]))
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC[PORT]))
    )
  )


  ※ $ lsnrctl status 했는데
  The listener supports no services 이런거 뜨면 

  DB에서 alter database register; 이거 실행 해주면 잘 뜸




  



● 2. 클라이언트 tnsnames.ora 파일 내용 확인

C:\Users\[PC계정명]> sqlplus [계정명/비밀번호]@[서버 아이피]:[포트]/[서비스 명]   -- 리스너 접속

C:\Users\[PC계정명]> path      -- DB 다운로드 받은 경로 추출하기  ( $경로/nework/admin )


  
-- tnsnames.ora 파일 안의 로컬 지정 내용

...
abc123 =        -- 밑에 설명 적기위해 아무렇게나 적은 것임
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = [서버 아이피])(PORT = [포트]))
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = [포트])
    )
  )
...



-- tnsping

C:\Users\[PC계정명]> tnsping abc123

TNS Ping Utility for 64-bit Windows: Version 19.0.0.0.0 - Production on 07-4월 -2026 20:17:30

Copyright (c) 1997, 2019, Oracle.  All rights reserved.

사용된 매개변수 파일:
D:\[경로]\network\admin\sqlnet.ora

별칭 분석을 위해 TNSNAMES 어댑터 사용
(DESCRIPTION = (ADDRESS = (PROTOCOL = TCP)(HOST = [서버 아이피])(PORT = [포트]))
   (CONNECT_DATA = (SERVER = DEDICATED) (SERVICE_NAME = [서비스 명]))) 에 접속하려고 시도하는 중
확인(20밀리초)  



-- 리스너 접속

C:\Users\[PC계정명]> sqlplus [계정ID/비밀번호]@abc123

select instance_name from v$instance;
INSTANCE_NAME
-------------
[ORACLE_SID]





  



● 3. 포트 번호 변경하기  ( 보안 이슈 )


-- 리스너를 내린 후 수정하고 다시 올림
  
$ cat listener.ora

LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST = [서버 아이피])(PORT = [다른 포트]))
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC[다른 포트]))
    )
  )

  

-- 다른 포트 방화벽 열기
  
$ vi f.sh
  
# 포트 허용 (영구 적용)
sudo firewall-cmd --permanent --add-port=[다른 포트]/tcp

# 설정 내용 반영
sudo firewall-cmd --reload

# 적용 확인
sudo firewall-cmd --list-ports | grep [다른 포트]




-- 새로운 포트를 사용하는 엔트리 ( 보통 $ORACLE_HOME/network/admin/ 에 위치)
  
MY_LISTENER =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = [서버IP or 호스트명])(PORT = [다른 포트]))
  )




-- DB 파라미터 수정

--시스템 레벨에서 로컬 리스너 주소 등록
ALTER SYSTEM SET LOCAL_LISTENER = 'MY_LISTENER' SCOPE=BOTH;

-- 즉시 서비스 등록 명령 실행
ALTER SYSTEM REGISTER;




-- 상태 확인

lsnrctl status

  
  
-- 접속 확인

$ sqlplus [계정명/비밀번호]@[서버 아이피]:[포트]/[서비스 명]











● 4. 서버 프로세서

- 접속 유저가 많으면 shared server 구조로 구성

# dedicated server 구조 : 유저 프로세서 1개  →  서버 프로세서 1개 할당
# shared server 구조    : 유저 프로세서와 서버 프로세서 사이에 디스패쳐가 일거리를 부여 ( 3-tier랑 비슷한건가 )



-- 서버 쪽 세팅

  (dispatcher 의 띄울 수 있도록 파라미터 설정)
alter system set dispatchers='(protocol=tcp)(dispatchers=1)' scope=both;

  (shared server process의 갯수를 결정하는 파라미터를 설정)
alter system set shared_servers=2 scope=both;
alter system set max_shared_servers=10 scope=both;

  (shared server process 를 사용할 세션의 갯수를 결정하는 파라미터를 설정 )
alter system set sessions=100 scope=spfile;
alter system set shared_server_sessions=95 scope=both;   -- (dedicated 서버 프로세스 5개 확보 : 특별 작업하는 세션을 위해)

  (dispatcher가 모자랄 경우를 대비해 자동으로 3개까지 늘어남)
alter system set max_dispatchers=3 scope=both; 
  
connect / as sysdba
shutdown immediate
startup


-- 현재 떠있는 디스패쳐의 갯수
select name, status from v$dispatcher;

-- 현재 떠있는 shared server 의 갯수 
select name, status from v$shared_server;


-- tnsnames.ora
abc123 =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = [서버 아이피])(PORT = [포트]))
    (CONNECT_DATA =
      (SERVER = SHARED)  -- ← shared 
      (SERVICE_NAME = [포트])
    )
  )



 
-- 파라미터 모아둔 것
  
1. dispatchers = 2
2. max_dispatchers = 6
3. shared_servers = 4
4. max_shared_servers = 8
5. sessions  = 200
6. max_shared_sessions = 195 









● 5. Shared Sever 구조 주의사항 1  ( 이미지 출처 : 나노바나나2 )
  

# Dedicated Server 구조의 SGAㆍPGA

<img width="1024" height="738" alt="image" src="https://github.com/user-attachments/assets/9004f16a-4bea-4f2f-8fe1-f0bda26ae84a" />



# Shared Sever 구조의 SGAㆍPGA
  
<img width="1024" height="694" alt="image" src="https://github.com/user-attachments/assets/c5db14fd-f65c-4b2b-ad5d-57115af5a930" />



-- Shared Sever 구조에서 UGA는 SGA 안으로 들어감
  여러 공유 서버 프로세서들의 유저에 대한 정보를 서로 공유
  large pool에 들어가기 때문에 large_pool_size를 기존보다 더 늘려야 



-- AMM(Automatic Memory Managment) 으로 관리되고 있는지 확인

show parameter memory_target
NAME          TYPE        VALUE
------------- ----------- -----
memory_target big integer 0

위의 값이 0 이면 AMM X


  
-- ASMM(Automatic Shared Memory Management) 으로 관리되고 있는지 확인

show parameter sga_target
NAME       TYPE        VALUE
---------- ----------- -----
sga_target big integer 2304M

show parameter pga
NAME                 TYPE        VALUE
-------------------- ----------- -----
pga_aggregate_limit big integer  2G
pga_aggregate_target big integer 767M



-- 사이즈 변경
alter system set large_pool_size=100m scope=both;








● 6. Shared Sever 구조 주의사항 2

- shared server process를 할당받아서 db를 startup과 shutdown 할 수 없음
  
select server from v$session where sid = sys_context('USERENV', 'SID');  
SERVER (SYSDBA 접속 시)       SERVER (리스너 접속 시)
---------                     ---------
DEDICATED                     SHARED


shutdown immediate  (리스너 접속 시) -- SYSDBA는 잘 
ORA-00106: cannot startup/shutdown database when connected to a dispatcher




※ 특정 아이피 접속 못하게 하기

$ cat sqlnet.ora
NAMES.DIRECTORY_PATH= (TNSNAMES, EZCONNECT)

ADR_BASE = [ORACLE_BASE]

tcp.validnode_checking=yes
tcp.invited_nodes=([아이피 1번],[아이피 2번],[아이피 3번])

SQLNET.AUTHENTICATION_SERVICES=(NONE)






  
  
</pre>
