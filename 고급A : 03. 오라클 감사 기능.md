<pre>

  
● 0. 감사 기능 : DB에서 누가, 언제, 무엇을, 어떻게 했는지를 추적하고 기록

  - 보안 사고 추적 : 해킹이나 내부자의 무단 접근이 발생했을 때 추적 가능
  - 내부 위협 방지 : DBA, 개발자처럼 권한이 높은 사용자도 감시 대상이 되며, 감사 자체가 불법 행위를 억제
  - 법적 규정 준수 : 금융, 의료, 공공기관 등은 관련 법령에 따라 접근 기록을 의무적으로 보관
  - 이상 징후 탐지 : 대량 조회, 심야 접속, 민감 테이블 접근 같은 비정상 패턴을 감지
  - 데이터 무결성  : 중요한 데이터의 변경, 삭제 이력을 남겨 원인 분석과 복구

</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

  
● 1. 필수 감사

-- 감사 모드 확인
show parameter audit_trail
NAME         TYPE    VALUE
------------ ------- ------
audit_trail  string  DB


-- 감사 활성화
audit table;


-- 나쁜 짓
drop table emp;


-- 누가 drop 했는 지 확인
select username, timestamp, action_name, terminal, userhost
  from dba_audit_trail;

  
-- 감사 비활성화
noaudit table;

</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>
  
● 2. 상세 감사 Fine-Grained Auditing (FGA) 

 - 일반 감사 : "누가 어떤 테이블에 접근했다" 수준          ex) scott 유저가 select * from emp;
 - 상세 감사 : "누가 어떤 조건의 데이터를 조회했다" 수준   ex) scott 유저가 select * from emp where sal >= 5000;

 - 컨텐츠 기반 모니터링 : 접근 여부, 특정 컬럼ㆍ특정 조건 감시
 - 특정열 지정          : sal 컬럼 조회할 때 감사기록 남기도록 설정 가능
 - 다양한 DML           : SELECT, DML (MERGE 포함) 모두 감사 가능
 - 프로시저 연동        : 감사 이벤트 발생 시 프로시저 자동 실행 시킬 수 있음 ( 관리자에게 알림 보내기도 가능 )
 - 패키지로 관리 가능   : 정책 추가, 삭제, (비)활성화  ( DBMS_FGA 패키지 )


ex)

-- 감사 설정 (SYS)
begin
    dbms_fga.add_policy( object_schema   => 'SCOTT'
                       , object_name     => 'EMP'
                       , policy_name     => 'POL7'
                       , audit_condition => 'SAL >= 3000'
                       , enable          => TRUE
                       , statement_types => 'INSERT,SELECT,UPDATE'
                       , audit_trail     => DBMS_FGA.DB_EXTENDED);
end;
/


  
-- 조회 (일반 유저)
select ename, sal from emp where ename = 'KING';
ENAME             SAL
---------- ----------
KING             5000


  
-- 상세감사 확인 (SYS)
select to_char(TIMESTAMP,'yyyy/mm/dd HH24:MI:SS') timeinfo
     , userhost, os_user, OBJECT_NAME,SCN, SQL_TEXT,SQL_BIND
  from dba_fga_audit_trail;

<img width="1445" height="325" alt="image" src="https://github.com/user-attachments/assets/d8556926-f87a-4837-8ef6-3f1b053fe7f1" />



-- 감사 정책 Drop (SYS)
begin
  dbms_fga.drop_policy( object_schema => 'SCOTT'
                      , object_name   => 'EMP'
                      , policy_name   => 'POL7' );
end;
/

</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>
  
● 3. SYSDBA / SYSOPER 감사

- 일반 감사 대상 제외 + 일반 감사는 DB가 열려 있어야 함 ( SYSDBA는 DB가 닫혀도 접속 가능 )
- DBA 감사 : OS 파일에 기록 ( DB 꺼져도 기록이 가능한 이유 )  →  DBA가 임의로 삭제ㆍ조작 불가
             AUDIT_SYS_OPERATIONS 파라미터 : true  →  SYSDBA / SYSOPER 의 모든 작업 기록
             DB 시작 / 종료도 기록 ( 일반 감사로는 절대 잡을 수 없는 DB 기동ㆍ종료ㆍ복구 작업까지 모두 감사 대상 )

- 접속한 OS 계정, 접속 시간, 실행한 명령어, 접속 방식(로컬/원격) 등 모두 기록
- 어떤 OS 사용자가 SYSDBA로 접속했는 지를 추적 가능  →  서버에 직접 접근한 내부자 감시 가능


[ORACLE_BASE]/admin/[ORACLE_SID]/adump -- 보통 경로

sqlplus / as sysdba -- 접속
select * from scott.emp;

  
 →  adump 폴더에 .aud 파일 생성

- 접속한 OS 계정명
- 접속 시간
- 실행한 SQL 명령어
- 로컬/원격 접속 방식
- 성공/실패 여부

  
※ DB 안에서 감사기록 보기 가능

DBA_AUDIT_TRAIL 뷰
V$XML_AUDIT_TRAIL 뷰


</pre>
