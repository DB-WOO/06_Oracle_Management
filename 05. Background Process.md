<pre>
<br>
● 1. DBWR 프로세스

  - 버퍼 캐시에서 변경된 더티 버퍼를 디스크의 DataFile로 기록
    ① 사용자 Update
    ② Buffer Cache의 Data Block 수정 → 해당 블록은 Dirty Buffer 상태
    ③ DBWR가 나중에 Datafile에 기록

      free buffer   : 비어있는 버퍼
      pinned buffer : 현재 사용중인 버퍼
      dirty buffer  : 메모리 변경 → 디스크와 일치 하지 않는 버퍼

  - 실행 시점
    ① 체크포인트 발생
       alter database checkpoint;
    ② 버퍼 캐시 공간 부족 시
    ③ 일정 시간 간격
    ④ 테이블 스페이스 OFFLINE or READ ONLY 전환 시
    ⑤ DB Shutdown Immediate

  - DBWR 느릴 경우
    ① Free Buffer Wait 발생
    ② DB Writer checkpoint wait
    ③ Dirty buffer 증가
    ④ 성능 저하




● 2. LGWR 프로세스

  - Redo Log Buffer에 기록된 변경 내역을 Redo Log File에 기록
  
  - 변경이력을 저장
    ① 장애 발생 시 redo log로 복구
    ② 트랜잭션 안정성 보장
    ③ COMMIT 성공 보장

  - 유저 UPDATE  →  Buffer Cache 블록 변경  →  Redo 정보 생성  →  Redo Log Buffer에 기록  →  LGWR : Redo Log File에 기록  →  COMMIT 완료

  - 실행 시점
    ① COMMIT; (LGWR가 REDO 정보를 디스크에 기록해야 완료)
    ② Redo Log Buffer가 1/3 이상 채워짐
    ③ 3초 주기
    ④ DBWR 프로세서 실행 전 (Write-Ahead Logging : 실제 DataFile 기록 전, Redo Log에 먼저 기록)

  select pname, spid from v$process where pname like 'LGW%';

DBWR : I/O Slaves 여러개 둘 수 있음 (병목현상 완화)
LGWR : 기본 1개





● 3. CKPT 프로세스

  - DB의 일관된 복구 기준 시점

  - CKPT 발생 시
    ① 데이터파일 헤더 업데이트 - 체크포인트 SCN
    ② 컨트롤 파일 업데이트     - 체크포인트 최신 정보
    ③ DBWR에게 기록 작업 지시

  - 장애 발생시 어디까지 저장됐는지 알 수 없음 (메모리 VS 디스크)
    → 체크포인트로 복구 기준 설정

  
SQL> show parameter checkpoint
NAME                      TYPE        VALUE
------------------------- ----------- -----
log_checkpoint_interval   integer     0      → Redo Log 블록수 기준 CKPT 발생 (0:비활성화)
log_checkpoint_timeout    integer     1800   → 마지막 CKPT 후 30분
log_checkpoints_to_alert  boolean     FALSE  → alert.log 기록 여부

SQL> alter system checkpoint;
SQL> select file#, checkpoint_change# from v$datafile_header;
SQL> select checkpoint_change# from v$database;




● 4. SMON 프로세스

  - Instance 장애 발생 후 DB Recovery 수행 

  - 자동 복구
    ① 서버 다운
    ② 전원 장애
    ③ Instance Crash
    ④ OS 강제 종료

  
  - 역할 : 커밋된 것만 유지해서 데이터 일관성 복구
  
    ① Instacne Recovery : 장애 발생 후 DB 재시작 시 수행  
       A 세션 : UPDATE (COMMIT X)
       B 세션 : Shudown abort 후 Startup → crash recovery 뜸
  
    ② Roll Forward      : Redo 로그 사용 → 변경사항 재적용
    ③ Roll Back         : 완료 x 트랜잭션 → 취소




● 5. PMON 프로세스

  - 유저 PC 비정상 종료 시, 뒷 정리 ( Rollback, Lock Release 등.. )
  - idle 세션 끊음
  - 리스너에게 동적 서비스 등록 (11g 이하) (12c 이후 : LREG)


※ 학습용으로 포트 영구 개방 방법
  sudo firewall-cmd --permanent --add-port=1234/tcp    -- 1234 포트 영구 개방
  sudo firewall-cmd --reload                           -- 설정 적용
  sudo firewall-cmd --list-ports                       -- 적용 여부확인
  sudo service iptables save                           -- 저장 (재부팅 시 유지)
  netstat -an | grep 1234                              -- 실제로 포트가 열렸는 지 확인

  학습용임, 현업에서 이거 함부로 쓰면 얻어 터질지도 모름




● 6. RECO 프로세스

  - 분산 DB,  A PC에서 B PC에 있는 다른 DB에 작업하다 비정상 종료 시 B에서 A가 쓰던 세션의 서버 프로세스를 정리 




● 7. ARCH 프로세스

  - Redo Log File이 다른 내용으로 덮어지기 전에 백업본 (Archive Log File)



<img width="1138" height="752" alt="image" src="https://github.com/user-attachments/assets/0774a20e-da4a-46de-a849-09cb3819d79a" />


# 아카이브 로그 모드로 변경

SQL> shutdown immediate
SQL> startup mount
SQL> alter database archivelog;  -- 변경
SQL> alter database open;

SQL> alter system set db_recovery_file_dest_size=1g;
SQL> alter system set db_recovery_file_dest='[ORACLE_HOME]/dbs/arch'; -- 아카이브 로그파일 생성 위치

SQL> select group#, status, sequence#, archived from v$log;  -- 아카이브 파일 상태 확인
    GROUP# STATUS    SEQUENCE# ARC
---------- --------- --------- ---
         1 INACTIVE         16 YES
         2 ACTIVE           17 YES
         3 CURRENT          18 NO

SQL> alter system switch logfile;  -- 로그 스위치 (아카이브 파일 수동 생성 + 체크포인트 이벤트)
SQL> select group#, status, sequence#, archived from v$log;
    GROUP# STATUS    SEQUENCE# ARC
---------- --------- --------- ---
         1 CURRENT          19 NO
         2 ACTIVE           17 YES
         3 ACTIVE           18 YES

  
[ 2026_03_22]$ pwd
[ORACLE_HOME]/dbs/arch/[ORACLE_SID]/archivelog/2026_03_22
[ 2026_03_22]$ ls
o1_mf_1_18_nvxvtr68_.arc  -- archived 컬럼이 NO 였던 18번이 파일로 만들어짐




</pre>
