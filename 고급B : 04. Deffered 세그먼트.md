<pre>

Deffered 테이블 : 수백 개의 테이블 생성 시 당장 공간 여유가 없으면, insert를 할때 Extent가 할당 됨

  segment creation immediate : 테이블 생성 시 바로 세그먼트 할당
  segment creation deferred  : 테이블 생성 후 insert 이후에 세그먼트 할
  

  
-- SEGMENT CREATION IMMEDIATE : 세그먼트 바로 생성

create table emp700
( empno number(10)
, ename varchar2(500) )
 segment creation immediate tablespace users;

select table_name
  from user_tables
 where table_name = 'EMP700';    -- EMP700

select segment_name, extents, blocks
  from user_segments
 where segment_name = 'EMP700';  -- EMP700  1  8



-- SEGMENT CREATION DEFERRED : 데이터가 없으면 세그먼트가 생성되지 않음 (sys에서 하니까 오 뜸)

create table emp800
( empno number(10)
, ename varchar2(500) )
segment creation deferred tablespace users;

select table_name
  from user_tables
 where table_name = 'EMP800';    -- EMP800

select segment_name, extents, blocks
  from user_segments
 where segment_name = 'EMP800';  -- no rows selected

insert into emp800 values (1111, 'aaaa');
-- 삽입 후 커밋도 안했는데, EMP800  1  8 뜸
-- 롤백 해봤는데, EMP800  1  8 뜸

  


  
※ 인덱스를 Deffered ?


  
create index emp_job on emp(job) segment creation deferred;
-- 테이블 Deffered 하면, 인덱스 생성도 자동으로 Deffered
-- 아니면 세션 파라미터를 바꾼 뒤 인덱스 생성
  
<img width="673" height="1021" alt="image" src="https://github.com/user-attachments/assets/07096443-40b6-49e7-bfc5-0117ebfd61a2" />
 
</pre>
