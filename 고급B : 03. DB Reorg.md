<pre>

● 0. DB Reorg : 테이블 / 인덱스를 다시 구성  →  성능 높임, 저장공간 확보

  
<img width="1024" height="683" alt="image" src="https://github.com/user-attachments/assets/41c6384b-f345-493a-9b28-9678d06f41ba" />
  
</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 1. Move

-- HWM 올리기
insert into emp select * from emp;
/  -- 10번 수행
commit;

-- 블록 번호 개수 : 85개
select count(distinct dbms_rowid.rowid_block_number(rowid)) blocks from emp;

-- DELETE 해도 HWM은 보통 안내려감 ( 여전히 85개 )
delete from emp where deptno in (10,20);
commit;
  

-- 테이블 스페이스 확인 후 신규 생성  →  Move
select table_name, tablespace_name
  from user_tables
 where table_name = 'EMP';

create tablespace ts101 datafile '[HOME]/ts101.dbf' size 50m;

alter table emp move tablespace ts101;


-- 블록 번호 개수 : 38개  →  내려감
select count(distinct dbms_rowid.rowid_block_number(rowid)) blocks from emp;


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 2. 인덱스가 있는 경우

  
인덱스가 있는 테이블을 다른 테이블 스페이스로 move 시켜서 db reorg 작업
→  반드시 해당 테이블의 인덱스도 rebuild
  
@demo  -- emp 테이블 다시 구성

-- 인덱스 생성
create index emp_sal on emp(sal);

-- HWM 올리기
insert into emp select * from emp;
/  -- 10번 수행
commit;

delete from emp where deptno  in ( 10, 20 ); 
commit;


  
-- 테이블 Move
alter table emp  move tablespace users; 

-- 인덱스로 조회  →  오류
select /*+ index(emp emp_sal) */ 
       ename, sal
  from emp
 where sal = 1600; 

ERROR at line 1: ORA-01502: index 'SCOTT.EMP_SAL' or partition of such index is in unusable
  


-- 인덱스 Rebuild
alter index emp_sal rebuild online; 

-- 다시 인덱스로 조회  →  성공
select /*+ index(emp emp_sal) */ 
       ename, sal
  from emp
 where sal = 1600; 


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 3. Shrink

DB Reorg의 문제점 : 테이블 move  →  Index Rebuild 빼먹으면 문제됨

- ALTER TABLE emp SHRINK SPACE COMPACT; : 비어있는 공간으로 data 옮김            (DML, DQL 실행 가능)
- ALTER TABLE emp SHRINK SPACE;         : 비어있는 공간으로 data 옮김 + HWM 내림 (DML 불가)


@demo.sql                                 -- 테스트 데이터 초기화

insert into emp select * from emp;        -- 10번 수행
commit; 

delete from emp where deptno in (10,20);  -- HWM 밑에 빈 공간 만들기
commit;


  
-- 사용중인 블록 : 169,  HWM 까지 할당된 블록 : 256
select count(distinct dbms_rowid.rowid_block_number(rowid)) as blocks
  from emp;

select blocks
  from user_segments
 where segment_name = 'EMP';


  
alter table emp enable row movement;
alter table emp shrink space compact;  -- COMPACT : 데이터 앞으로 모으


  
-- 사용중인 블록 : 77,  HWM 까지 할당된 블록 : 256  →  앞으로 압축만 되었음 
select count(distinct dbms_rowid.rowid_block_number(rowid)) as blocks
  from emp;

select blocks
from user_segments
where segment_name = 'EMP';



-- HWM 내린 후, HWM 까지 할당된 블록 : 88  →  HWM 내려감
alter table emp shrink space;

select blocks
  from user_segments
 where segment_name = 'EMP';

</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 4. 테이블 압




</pre>
