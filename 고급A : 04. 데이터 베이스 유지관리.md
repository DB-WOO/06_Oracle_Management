<pre>

● 1. AWR 레포트 ( Automatic Workload Repository )

  - 성능 정보에 대한 내장 Repository
  - 1시간 마다 DB Metrics의 스냅샷 생성  →  8일간 보관
  - 모든 자체 관리 기능의 토대


-- AWR 설정 확인
SELECT SNAP_INTERVAL, RETENTION, TOPNSQL
  FROM DBA_HIST_WR_CONTROL;

SNAP_INTERVAL            RETENTION               TOPNSQL
+00000 01:00:00.0        +00008 00:00:00.0       DEFAULT
1시간마다 스냅샷 수집     8일간 보관              상위 SQL 30개 저장

-- AWR 설정 변경
BEGIN
  DBMS_WORKLOAD_REPOSITORY.MODIFY_SNAPSHOT_SETTINGS
  ( interval  => 60     -- 분 단위
  , retention => 11520  -- 8일 = 60*24*8  );
END;
/

-- 수동 스냅샷 수집
EXEC DBMS_WORKLOAD_REPOSITORY.CREATE_SNAPSHOT;

-- 스냅샷 목록
SELECT SNAP_ID
     , TO_CHAR(BEGIN_INTERVAL_TIME, 'YYYY-MM-DD HH24:MI') AS BEGIN_TIME
     , TO_CHAR(END_INTERVAL_TIME,   'YYYY-MM-DD HH24:MI') AS END_TIME
  FROM DBA_HIST_SNAPSHOT
 ORDER BY SNAP_ID DESC
 FETCH FIRST 10 ROWS ONLY;


-- 스냅샷 자동 수집 세팅 확인
show parameter statistics_level
NAME                                 TYPE        VALUE
------------------------------------ ----------- ------------------------------
client_statistics_level              string      TYPICAL
statistics_level                     string      all


-- 악성 SQL (14^10 건)
SELECT COUNT(*) FROM SCOTT.EMP, SCOTT.EMP, SCOTT.EMP, SCOTT.EMP, SCOTT.EMP
                   , SCOTT.EMP, SCOTT.EMP, SCOTT.EMP, SCOTT.EMP, SCOTT.EMP;
  

-- 리눅스에서 CPU를 많이 쓰는 SPID 확인 ( TOP 명령어 )
  
<img width="1725" height="456" alt="image" src="https://github.com/user-attachments/assets/ba4511b8-22ff-4edc-b3e0-44aaa0976b2f" />




-- 오렌지 > Session Monitor > SPID 확인 후 선택 > DBA > AWR Manager > Main Report > SQL Statistics

<img width="1819" height="504" alt="image" src="https://github.com/user-attachments/assets/684448f4-e601-43e9-84e5-feaeb5f0cbe2" />



</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 2. 현재 데이터베이스 이슈 확인

 - Alert : Metric 기반 : 수치가 임계값을 넘으면 발생, 내려오면 자동으로 지워짐 (임계값)
           이벤트 기반 : 이벤트 발생 시 Alert, 자동으로 지워지지 않음

 - MMON : AWR 스냅샷 자동 수집
          매트릭 수치를 임계값과 주기적 비교 
          임계값 초과 시 DBA_OUTSTANDING_ALERTS에 Alert를 등록
          수치 회복 시 Alert를 자동으로 CLEAR 처리

 - 뷰 : DBA_OUTSTANDING_ALERTS : 현재 미해소 Alert (지금 당장 문제가 있는 것)
        DBA_ALERT_HISTORY      : 과거에 발생했다가 해소된 Alert 이력


-- sys
create tablespace ts300 datafile '[HOME]/oradata/ts300.dbf' size 1m;   -- 1MB

-- scott
create table emp300
( empno number(10)
, ename varchar2(10)
, sal   number(10) )
tablespace ts300;

insert into emp300 values(1111, 'aaa', 3000);
insert into emp300 select * from emp300;
/  ←  에러날 때 까지 계속

ERROR at line 1: ORA-01653: unable to extend table SCOTT.EMP300 by 8 in tablespace TS300


-- 이슈 확인 
SELECT * FROM DBA_OUTSTANDING_ALERTS;  -- 현재 이슈 확인
SELECT * FROM DBA_ALERT_HISTORY;       -- 해결된 Alert 이력 (시간 지나면 기록됨)


-- 해결하기 (공간 늘리기)
alter tablespace ts300 add datafile '[HOME]/oradata/ts300b.dbf' size 100m;



</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 3. 밤 10시에 자동 수행되는 업무

-- 옵티마이저 통계 수집
-- 통계 수집 대상 확인 (오래된 통계 or 통계 없는 테이블)
SELECT OWNER
     , TABLE_NAME
     , LAST_ANALYZED
     , NUM_ROWS
     , STALE_STATS
  FROM DBA_TAB_STATISTICS
 WHERE STALE_STATS = 'YES'
   AND OWNER NOT IN ('SYS','SYSTEM')
 ORDER BY LAST_ANALYZED NULLS FIRST;

  ① 테이블/인덱스의 통계 정보가 오래되거나 없는 경우 자동으로 갱신
  ② 통계가 없거나 10% 이상 데이터 변경이 있을 시
  ③ 이 작업이 안 돌면 실행계획 이상하게 만들어 짐
  ④ 수동으로 통계를 고정(LOCK)해둔 테이블은 건너뜀


-- Automatic Segment Advisor
-- Segment Advisor 권고 결과 확인
SELECT f.TASK_NAME
     , o.ATTR2     AS TABLE_NAME
     , f.MESSAGE
     , f.MORE_INFO
  FROM DBA_ADVISOR_FINDINGS f
  JOIN DBA_ADVISOR_OBJECTS  o ON f.TASK_ID = o.TASK_ID
 WHERE f.TASK_NAME LIKE 'SYS_AUTO_SEG%'
 ORDER BY f.TASK_ID DESC;

  ① 테이블/인덱스의 낭비 공간을 분석하고 SHRINK 권고
  ② DELETE가 많은 테이블은 공간을 반납하지 않고 HWM 위로 빈 블록이 쌓임
  ③ Advisor가 이를 감지하면 ALTER TABLE ... SHRINK SPACE 명령을 권고
  ④ 직접 실행은 해주지 않고 권고  →  DBA가 판단 후 직접 실행


-- Automatic SQL Advisor
-- 자동 SQL Tuning Advisor 결과 확인
SELECT t.TASK_NAME
     , t.STATUS
     , f.MESSAGE
     , f.MORE_INFO
  FROM DBA_ADVISOR_TASKS    t
  JOIN DBA_ADVISOR_FINDINGS f ON t.TASK_ID = f.TASK_ID
 WHERE t.ADVISOR_NAME = 'SQL Tuning Advisor'
   AND t.TASK_NAME LIKE 'SYS_AUTO%'
 ORDER BY t.CREATED DESC
 FETCH FIRST 20 ROWS ONLY;

-- SQL Profile 권고가 있으면 자동 적용 여부 확인
SELECT NAME,
     , TYPE,
     , STATUS,
     , SQL_TEXT
  FROM DBA_SQL_PROFILES
 ORDER BY CREATED DESC;

  ① AWR에 기록된 과부하 SQL을 자동으로 분석하고 튜닝 권고안 생성
  ② AWR Top SQL 중 반복 실행되는 고부하 SQL을 자동 선별하여 분석
  ③ 권고 유형이 **SQL Profile**인 경우 자동 적용(Accept) 가능
  ④ 인덱스 생성 권고는 자동 적용되지 않으므로 DBA가 직접 판단

-- Maintenance Window 확인 및 관리
-- Maintenance Window 스케줄 확인
SELECT WINDOW_NAME,
     , TO_CHAR(NEXT_START_DATE, 'YYYY-MM-DD HH24:MI') AS NEXT_START,
     ,  DURATION,
     , ENABLED
  FROM DBA_SCHEDULER_WINDOWS
 ORDER BY NEXT_START_DATE;

-- Autotask 3가지 작업 활성화 상태 확인
SELECT CLIENT_NAME
     , STATUS
  FROM DBA_AUTOTASK_CLIENT;  



작업                  하는 일                            직접 조치 필요?
옵티마이저 통계 수집  실행 계획의 재료가 되는 통계 갱신   불필요 (자동 적용)
Segment Advisor       낭비 공간 찾아서 SHRINK 권고       필요 (DBA가 직접 실행)
SQL Tuning Advisor    고부하 SQL 분석 후 튜닝 권고       필요 (Profile만 자동 적용 가능)




※ 스키마 통계정보 수집
dbms_stats.gather_schema_stats('SCOTT');



</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 4. 오라클 성능 레포트

레포트         핵심 질문                    시간 단위        해결책 제시
AWR            그 시간에 뭐가 문제였나?     스냅샷 구간      없음 (데이터만)
ADDM           원인이 뭐고 어떻게 고치나?   스냅샷 구간      있음
ASH            방금 전 무슨 일이 있었나?    초 단위          없음
AWR 비교       정상 때와 얼마나 차이나나?   두 구간 비교     없음



exec dbms_workload_repository.create_snapshot;  -- 수동 스냅샷

-- 부하
declare
 type rc is ref cursor;
 l_rc rc;
 l_dummy all_objects.object_name%type;
 l_start number default dbms_utility.get_time;
begin
 for i in 1 .. 10000
 loop
  open l_rc for
  'select object_name from all_objects where object_id = ' || i;
  fetch l_rc into l_dummy;
  close l_rc;
 end loop;
 dbms_output.put_line ( round( (dbms_utility.get_time - l_start)/100, 2) || 'seconds');
end;
/

exec dbms_workload_repository.create_snapshot;  -- 수동 스냅샷
@?/rdbms/admin/addmrpt.sql                      -- ADDM Report 생성



<pre>
