[sample.csv](https://github.com/user-attachments/files/26658771/sample.csv)<pre>

● 0. 오라클의 데이터 이행 방법

① Direct Load Insert
   서브쿼리 INSERT
   /*+ append */ : HWM 위로 적재
   AS-IS → TO-BE 변환 : Direct load insert
      컬럼 추가/삭제, 마스킹/암호화, 데이터 정제
      서브쿼리로 변환 로직을 같이 태움

② SQL*Loader
   CSV / Text 파일로 DB 적재
   대량 적재에 많이 사용

③ Export / Import ( Data Pump )
   테이블ㆍ스키마ㆍ테이블스페이스 단위로 이행
   운영 이행에서 가장 흔한 방식 ★

④ Oracle Golden Gate
   데이터 이행 및 동기화 툴
   비쌈

</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 1. Direct Load Insert
  
구분          일반 INSERT                            Direct-path INSERT(append)
적재 위치     기존 블록의 빈 공간(가능하면 재사용)    주로 HWM 위 신규 블록
속도          상대적으로 느릴 수 있음                 대량 적재에서 빠를 수 있음
Redo/Undo     일반적으로 생성                        설정/모드에 따라 동작이 달라질 수 있음(로그/복구 관점 체크)
커밋          커밋 전에도 입력한 데이터를 볼수 있음   커밋을 해야 입력한 데이터를 엑세스 할수 있음


<img width="457" height="439" alt="image" src="https://github.com/user-attachments/assets/c3a76dd8-fd05-4377-96ae-4e0227fd9377" />




create table emp205 
    as
select * from emp where 1 = 2;

insert /*+ append */ into emp205
select * from emp;

( Direct Path로 넣고 Commit 안함 )
  
select * from emp205;
ERROR at line 1: ORA-12838: cannot read/modify an object after modifying it in parallel

→ commit을 해야 HWM 위로 올라가서 읽을 수 있음




※ 병렬 INSERT

ALTER SESSION ENABLE PARALLEL DML;

INSERT /*+ PARALLEL(EMP205 4) */ INTO EMP205
SELECT * FROM EMP;

COMMIT;

ALTER SESSION DISABLE PARALLEL DML;
  
</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 2. SQL*Loader : 외부파일 (.txt .csv)에 있는 데이터를 오라클 테이블로 대량 적재해주는 유틸리티

  - 다른 시스템의 데이터를 오라클로 옮기거나 파일로 받은 데이터를 한번에 적재
  - 로딩 규칙은 .ctl 파일(제어 파일)에서 정의
  - 컬럼 매핑, 구분자, 날짜 형식, NULL 처리 지정 가능
  - 동작 방식에 따라 일반 로드(Conventional Path), 고속 로드(Direct Path)로 나뉨


구성 파일

구성 요소             설명
Data file             CSV, Text, Excel 등 적재할 원본 데이터 파일
Control file (.ctl)   SQL*Loader가 읽는 제어 파일 (컬럼 매핑, 구분자, 날짜 형식, NULL 처리 등 로딩 규칙)
Bad file (.bad)       문법 오류 등으로 인해 로딩이 거부된 데이터
Discard file (.dsc)   문법은 맞지만 테이블 제약조건 등으로 인해 적재가 거부된 데이터
Log file (.log)       로딩 과정에서 수행된 작업 이력 (성공, 실패, 건수 등)




<a href='https://github.com/user-attachments/files/26658772/sample.csv'>sample.csv</a>

→ 리눅스 : [HOME] 위치에 업로드


-- 테이블 생성
create table sample
( line_no    varchar2(20)
, time_inout varchar2(10)
, in_cnt     varchar2(10)
, out_cnt    varchar2(10) );



-- 컨트롤 파일 생성

$ vi sample.txt
  
options(skip=1)                    -- 첫번째행(컬럼명)은 생략
load data
infile '[HOME]/sample.csv'         -- 입력할 데이터 파일 위치와 이름 
into table sample                  -- 입력할 테이블명 
fields terminated by ','           -- 구분자를 콤마(,)
optionally enclosed by '"'         -- 옵션으로 데이터 중에서 더블 쿼테이션 마크가 있을 수 있음
(line_no, time_inout, in_cnt, out_cnt)



-- 데이터 이행
$ sqlldr [DB 유저명]/[DB 유저PW] control=sample.txt data=sample.csv -- direct=y 옵션 추가 시 Direct Path 방식으로 Load



-- 확인
$ cat sample.log

</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 3. Export / Import

  - Dump : 단순하고 빠르게 데이터 이행
  - Pump : 대용량 데이터를 빠르게 이행 (약간 복잡)

  - Table Level      : 특정 테이블 Export / Import
  - User Levle       : 유저가 가지고 있는 모든 객체 Export / Import
  - TableSpace Level : 특정 테이블 스페이스를 통째로 Export / Import
  - Database Level   : Database를 통째로 Export / Import

</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 3-1. Dump - Table Level
   
-- AS-IS : 12c -------------------------------------------------------------------------------------------

-- Export
$ exp file=hr_emp.dmp tables=employees    -- 사용자명/비밀번호 : hr/hr

-- 전송
$ scp hr_emp.dmp [TO-BE OS 계정]@[TO-BE IP]:[TO-BE에서 Import할 디렉토리]    -- yes / TO-BE의 비밀번호

----------------------------------------------------------------------------------------------------------

-- TO-BE : 19c -------------------------------------------------------------------------------------------

-- 테이블 스페이스와 유저 생성

create tablespace hr2_ts datafile '[HOME]oradata/hr2_ts01.dbf' size 100m;

grant connect,resource to hr2;
alter user hr2 default tablespace hr2_ts;
alter user hr2 quota unlimited on hr2_ts;
alter user hr2 quota unlimited on sysaux;

-- Import
$ imp system/oracle_4U file=hr_emp.dmp fromuser=hr touser=hr2 tables=employees constraints=n ignore=y

select count(*) from employees; -- 107건
   
----------------------------------------------------------------------------------------------------------


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 3-2. Dump - User Level

-- AS-IS : 12c -------------------------------------------------------------------------------------------

-- Export
$ exp system/oracle file=hr_schema.dmp owner=hr

-- 전송
scp hr_schema.dmp [TO-BE OS 계정]@[TO-BE IP]:[TO-BE에서 Import할 디렉토리]
   
----------------------------------------------------------------------------------------------------------

-- TO-BE : 19c -------------------------------------------------------------------------------------------

-- 테이블 스페이스와 유저 생성

create tablespace hr2_ts datafile '[HOME]/oradata/hr2_ts01.dbf' size 100m;

grant connect,resource to hr2;
alter user hr2 default tablespace hr2_ts;
alter user hr2 quota unlimited on hr2_ts;
alter user hr2 quota unlimited on sysaux;
   
-- Import
$ imp system/oracle_4U file=hr_schema.dmp fromuser=hr touser=hr2 ignore=y

----------------------------------------------------------------------------------------------------------


   
※ 데이터가 잘 넘어왔는지 검증 

-- 방화벽 오픈 (12c)
# firewall-cmd --permanent --add-port=1521/tcp
# firewall-cmd --reload

$ cd [ORACLE_HOME]network/admin
$ cat listener.ora

LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST = [AS-IS IP])(PORT = [PORT]))
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC[PORT]))
    )
  )

$ lsnrctl status   -- 리스너 확인 (인스턴스 확인)



-- tnsping (19c)
$ tnsping [AS-IS IP]:[AS-IS PORT]/[AS-IS Service Name]



-- 링크 생성 (19c → 12c)
   
$ sqlplus / as sysdba
grant create database link to hr2;
   
connect hr2/hr2
   
create database link hr_link
connect to hr
identified by hr
using '[AS-IS IP]:[AS-IS PORT]/[AS-IS Service Name]';

select * from tab@hr_link;



-- 오브젝트 차이 확인 + 건수 확인 ---------------------------------------------------------

- 1. 테이블 차이 비교
select table_name from user_tables
minus
select table_name from user_tables@hr_link;

select table_name from user_tables@hr_link
minus
select table_name from user_tables;

-- 2. 인덱스 차이 비교
select index_name from user_indexes
minus
select index_name from user_indexes@hr_link;

select index_name from user_indexes@hr_link
minus
select index_name from user_indexes;

-- 3. 제약 차이 비교
select constraint_name from user_constraints
minus
select constraint_name from user_constraints@hr_link;

select constraint_name from user_constraints@hr_link
minus
select constraint_name from user_constraints;

-- 4. 시퀀스 차이 비교
select sequence_name from user_sequences
minus
select sequence_name from user_sequences@hr_link;

select sequence_name from user_sequences@hr_link
minus
select sequence_name from user_sequences;

-- 5. 시너님 차이 비교
select synonym_name from user_synonyms
minus
select synonym_name from user_synonyms@hr_link;

select synonym_name from user_synonyms@hr_link
minus
select synonym_name from user_synonyms;

-- 6. 뷰 차이 비교
select view_name from user_views
minus
select view_name from user_views@hr_link;

select view_name from user_views@hr_link
minus
select view_name from user_views;


-- 7. 건수 비교
select table_name, num_rows
  from user_tables
 minus
select table_name, num_rows
  from user_tables@hr_link;

select table_name, num_rows
  from user_tables@hr_link
 minus
select table_name, num_rows
  from user_tables;

------------------------------------------------------------------------------------------


</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 3-3. Dump - Tablespace level

-- AS-IS : 12c -------------------------------------------------------------------------------------------

======== 테스트용으로 생성ㆍ권한 부여 =============================

create tablespace ts700 datafile '[ORACLE_BASE]/oradata/[ORACLE_SID]/ts700.dbf' size 100m;

create user scott700 identified by tiger;
grant connect, resource to scott700;

alter user scott700 default tablespace ts700;
alter user scott700 quota unlimited on ts700;


connect scott700/tiger

create table dept
( deptno number(10)
, dname  varchar2(14)
, loc    varchar2(13) )
tablespace ts700;

create table emp
( empno    number(4) not null
, ename    varchar2(10)
, job      varchar2(9)
, mgr      number(4)
, hiredate date
, sal      number(7,2)
, comm     number(7,2)
, deptno   number(2) )
tablespace ts700;

insert into dept values (10, 'ACCOUNTING', 'NEW YORK');
insert into dept values (20, 'RESEARCH',   'DALLAS');
insert into dept values (30, 'SALES',      'CHICAGO');
insert into dept values (40, 'OPERATIONS', 'BOSTON');

insert into emp values (7839,'KING','PRESIDENT',null,'81-11-17',5000,null,10);
insert into emp values (7698,'BLAKE','MANAGER',7839,'81-05-01',2850,null,30);
insert into emp values (7782,'CLARK','MANAGER',7839,'81-05-09',2450,null,10);
insert into emp values (7566,'JONES','MANAGER',7839,'81-04-01',2975,null,20);
insert into emp values (7654,'MARTIN','SALESMAN',7698,'81-09-10',1250,1400,30);
insert into emp values (7499,'ALLEN','SALESMAN',7698,'81-02-11',1600,300,30);
insert into emp values (7844,'TURNER','SALESMAN',7698,'81-08-21',1500,0,30);
insert into emp values (7900,'JAMES','CLERK',7698,'81-12-11',950,null,30);
insert into emp values (7521,'WARD','SALESMAN',7698,'81-02-23',1250,500,30);
insert into emp values (7902,'FORD','ANALYST',7566,'81-12-11',3000,null,20);
insert into emp values (7369,'SMITH','CLERK',7902,'80-12-09',800,null,20);
insert into emp values (7788,'SCOTT','ANALYST',7566,'82-12-22',3000,null,20);
insert into emp values (7876,'ADAMS','CLERK',7788,'83-01-15',1100,null,20);
insert into emp values (7934,'MILLER','CLERK',7782,'82-01-11',1300,null,10);

create index emp_sal on emp(sal);
create index emp_job on emp(job);

 alter table dept add constraint dept_pk primary key(deptno);

create sequence seq1
 start with 1
 increment by 1
 maxvalue 100;  
==================================================================


-- 테스트용 테이블 스페이스 READ ONLY

connect / as sysdba

select t.name, d.enabled
  from v$tablespace t, v$datafile d
 where t.ts# = d.ts#;

alter tablespace ts700 read only;

select t.name, d.enabled
  from v$tablespace t, v$datafile d
 where t.ts# = d.ts#;



-- Export
$ exp transport_tablespace=y tablespaces=ts700 file=ts700.dmp  

-- Dump File 전송
scp ts700.dmp [TO-BE OS 계정]@[TO-BE IP]:[TO-BE Import 받을 디렉토리]

-- Data File 전송
scp [ORACLE_BASE]/oradata/[ORACLE_SID]/ts700.dbf [TO-BE OS 계정]@[TO-BE IP]:[TO-BE Import 받을 디렉토리]

----------------------------------------------------------------------------------------------------------

-- TO-BE : 19c -------------------------------------------------------------------------------------------

connect / as sysdba

create user scott700 identified by tiger;
grant connect, resource to scott700;


-- Import
$ imp transport_tablespace=y file=ts700.dmp datafiles='[HOME]/oradata/ts700.dbf'


-- 이행 결과 확인
   
alter tablespace ts700 read write;

connect scott700/tiger
select table_name from user_tables;
select index_name from user_indexes;
   
----------------------------------------------------------------------------------------------------------
   
</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 4-1. Pump - Table Level

Pump의 장점

- 세밀한 선택 : 객체/데이터를 더 세부 단위로 선택해서 export/import 가능
- DB 버전 명시 : 특정 데이터베이스 버전을 지정하여 호환성 관리 가능
- 병렬 실행: 작업을 병렬로 수행해 처리 속도 향상
- 작업 공간 사용량 예측 : export 작업에 필요한 공간 소모량을 예측 가능
- 분산 환경 네트워크 모드 : 네트워크 모드로 원격 DB 간 이행 지원
- 리매핑 : 스키마, 테이블스페이스, 테이블명 등의 매핑 변경 지원
- 압축 지원 : 메타데이터 압축, export 중 데이터 압축 등으로 저장 공간 절감
- 보안 : encryption을 통해 덤프 파일 보안 강화
- XMLType 처리 : XMLType 데이터를 CLOB 형태로 export 지원
- 기존 모드 지원 : 이전 import/export 파일을 지원하는 호환(레거시) 모드 제공



-- AS-IS : 12c -------------------------------------------------------------------------------------------

-- 디렉토리 생성
$ pwd
/home/oracle/pump_ora12c


-- DB에서 펌프 디렉토리 생성
create directory datapump_dir as '[HOME]/pump_ora12c';
select * from dba_directories where directory_name='DATAPUMP_DIR';
   

-- HR에 방금 생성한 디렉토리의 권한 부여
grant read,write on directory datapump_dir to hr; 


-- Export
$ expdp hr/hr directory=datapump_dir tables=employees dumpfile=emp_pump.dmp


-- 전송
$ scp emp_pump.dmp [TO-BE OS 계정]@[TO-BE IP]:[TO-BE Import할 디렉토리]
   
----------------------------------------------------------------------------------------------------------
   
-- TO-BE : 19c -------------------------------------------------------------------------------------------

-- 디렉토리 생성
$ pwd
/home/oracle/pump_ora19c


-- DB에서 펌프 디렉토리 생성
create directory datapump_dir as '/home/oracle/pump_ora19c'; 


-- 디렉토리 사용 권한 부여
create user hr2 identified by hr2;
grant connect, resource  to hr2;
grant read,write on directory datapump_dir to hr2; 


-- 테이블 스페이스 생성ㆍ권한
create tablespace ts100 datafile '[HOME]/oradata/ts100.dbf' size 100m;
 
alter user hr2 default tablespace ts100;

alter user hr2 quota unlimited on ts100;


   
-- move
$ mv emp_pump.dmp /home/oracle/pump_ora19c/



-- Import
$ impdp hr2/hr2 directory=datapump_dir dumpfile=emp_pump.dmp remap_schema=hr:hr2 remap_tablespace=sysaux:ts100



-- Import 확인

select count(*) from employees;
select table_name, tablespace_name from user_tables;
   
----------------------------------------------------------------------------------------------------------

   
   
</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 4-2. Pump - User Level

-- AS-IS : 12c -------------------------------------------------------------------------------------------

-- HR 스키마 Export
$ expdp system/oracle \ directory=datapump_dir \ schemas=hr \ dumpfile=hr_pump.dmp
   
-- 전송
$ scp [HOME]/pump_ora12c/hr_pump.dmp \ [TO-BE OS 계정]@[TO-BE IP]:[TO-BE Directory]
   
----------------------------------------------------------------------------------------------------------

-- TO-BE : 19c -------------------------------------------------------------------------------------------

-- Import  
$ impdp system/oracle_4U \ directory=datapump_dir \ dumpfile=hr_pump.dmp \ remap_schema=hr:hr
        \ remap_tablespace=sysaux:ts100


-- 링크 생성 후 확인하기
CREATE DATABASE LINK hr_link
   CONNECT TO hr
IDENTIFIED BY hr
       USING '[AS-IS IP]:[AS-IS PORT]/[AS-IS Servive Name]';
   
=========================================================================

-- 1. 테이블 차이
SELECT table_name FROM user_tables
MINUS
SELECT table_name FROM user_tables@hr_link;

SELECT table_name FROM user_tables@hr_link
MINUS
SELECT table_name FROM user_tables;

-- 2. 인덱스 차이
SELECT index_name FROM user_indexes
MINUS
SELECT index_name FROM user_indexes@hr_link;

SELECT index_name FROM user_indexes@hr_link
MINUS
SELECT index_name FROM user_indexes;

-- 3. 제약 차이
SELECT constraint_name FROM user_constraints
MINUS
SELECT constraint_name FROM user_constraints@hr_link;

SELECT constraint_name FROM user_constraints@hr_link
MINUS
SELECT constraint_name FROM user_constraints;

-- 4. 시퀀스 차이
SELECT sequence_name FROM user_sequences
MINUS
SELECT sequence_name FROM user_sequences@hr_link;

SELECT sequence_name FROM user_sequences@hr_link
MINUS
SELECT sequence_name FROM user_sequences;

-- 5. 시너님 차이
SELECT synonym_name FROM user_synonyms
MINUS
SELECT synonym_name FROM user_synonyms@hr_link;

SELECT synonym_name FROM user_synonyms@hr_link
MINUS
SELECT synonym_name FROM user_synonyms;

-- 6. 뷰 차이
SELECT view_name FROM user_views
MINUS
SELECT view_name FROM user_views@hr_link;

SELECT view_name FROM user_views@hr_link
MINUS
SELECT view_name FROM user_views;

=========================================================================
   
----------------------------------------------------------------------------------------------------------

   
</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 4-3. Pump - Tablespace Level

※ 용어 정리
  - 데이터 이행
     기존 시스템에서 새 시스템으로 전환하기 위한 전체 전환 프로젝트 (추출, 변환, 적재, 검증, 컷오버)

  - 데이터 이관
     데이터를 한 위치에서 다른 위치로 복사/이동하는 행위 자체(변환은 최소)  (Data Pump)

  - TTS : Transport TableSpace


※ TTS 수행 전 확인 사항 (AS-IS  TO-BE  Character-Set이 동일해야 함)

SELECT *
  FROM database_properties
 WHERE property_name = 'NLS_CHARACTERSET';

SELECT *
  FROM database_properties
 WHERE property_name = 'NLS_NCHAR_CHARACTERSET';



-- AS-IS : 12c -------------------------------------------------------------------------------------------

-- 테이블 스페이스 생성
CREATE TABLESPACE ts10000 DATAFILE '[ORACLE_BASE]/oradata/[ORACLE_SID]/ts10000.dbf' SIZE 100m;

   
-- 필요 시 사용자 생성
CREATE USER hr IDENTIFIED BY hr
    DEFAULT TABLESPACE ts10000
  TEMPORARY TABLESPACE temp
  QUOTA UNLIMITED ON ts10000;

GRANT CONNECT, RESOURCE TO hr;
GRANT CREATE VIEW TO hr;
GRANT CREATE SEQUENCE TO hr;


-- hr 스크립트 위치 확인
$ find $ORACLE_HOME -name "hr_main.sql" 2>/dev/null


-- 스키마 설치 실행
$ cd $ORACLE_HOME/demo/schema/human_resources
@hr_main.sql hr ts10000 temp [HOME]/ 


====== 설치 확인 ==================================================

SELECT table_name
  FROM user_tables
 ORDER BY table_name;


-- 테이블별 건수   
SELECT 'countries'   , COUNT(*) FROM hr.countries
UNION ALL
SELECT 'departments' , COUNT(*) FROM hr.departments
UNION ALL
SELECT 'employees'   , COUNT(*) FROM hr.employees
UNION ALL
SELECT 'job_history' , COUNT(*) FROM hr.job_history
UNION ALL
SELECT 'jobs'        , COUNT(*) FROM hr.jobs
UNION ALL
SELECT 'locations'   , COUNT(*) FROM hr.locations
UNION ALL
SELECT 'regions'     , COUNT(*) FROM hr.regions;

-- 제약조건
SELECT table_name, constraint_name, constraint_type, status
FROM   user_constraints
ORDER BY table_name, constraint_type;

-- 인덱스
SELECT table_name, index_name, index_type, status
FROM   user_indexes
ORDER BY table_name;

-- 뷰
SELECT view_name
FROM   user_views
ORDER BY view_name;

-- 시퀀스
SELECT sequence_name, min_value, max_value, increment_by, last_number
FROM   user_sequences
ORDER BY sequence_name;

-- 프로그램 단위
SELECT object_name, object_type, status
FROM   user_objects
WHERE  object_type IN ('PROCEDURE','FUNCTION','PACKAGE','TRIGGER')
ORDER BY object_type, object_name;

-- 테이블스페이스
SELECT segment_name, tablespace_name
FROM   user_segments
WHERE  segment_type = 'TABLE'
ORDER BY segment_name;


===================================================================




TTS Export


-- Read Only
ALTER TABLESPACE ts10000 READ ONLY;

SELECT t.name, d.enabled, d.name
  FROM v$tablespace t, v$datafile d
 WHERE t.ts# = d.ts#;


-- Export
$ expdp directory=datapump_dir dumpfile=ts10000.dmp transport_tablespace=y tablespaces=ts10000

-- 덤프 파일 전송
scp [HOME]/pump_ora12c/ts10000.dmp [TO-BE OS 계정]@[TO-BE IP]:[HOME]/pump_ora19c/

-- 데이터 파일 전송
scp [ORACLE_BASE]/oradata/[ORACLE_SID]/ts10000.dbf [TO-BE OS 계정]@[TO-BE IP]:[HOME]/oradata


----------------------------------------------------------------------------------------------------------

-- TO-BE : 19c -------------------------------------------------------------------------------------------

-- 사용자 생성
CREATE USER hr2
  IDENTIFIED BY hr2
  DEFAULT TABLESPACE users
  TEMPORARY TABLESPACE temp
  QUOTA UNLIMITED ON users;

GRANT CONNECT, RESOURCE TO hr2;
GRANT CREATE VIEW TO hr2;
GRANT CREATE SEQUENCE TO hr2;

   
-- Import
$ impdp transport_datafiles='[HOME]/oradata/ts10000.dbf' directory=datapump_dir dumpfile=ts10000.dmp remap_schema=hr:hr2

※ 트리거 컴파일 오류 발생 할 수 있으나, 이관 자제는 성공할 수있음


-- tablespace READ WRITE 복구
ALTER TABLESPACE ts10000 READ WRITE;

GRANT CREATE DATABASE LINK TO hr2;
ALTER USER hr2 IDENTIFIED BY hr2;


----------------------------------------------------------------------------------------------------------


-- 19c에서 링크 생성 후 확인

CREATE DATABASE LINK hr_link
CONNECT TO hr
IDENTIFIED BY hr
USING '[AS-IS IP]:[AS-IS PORT]/[AS-IS 서비스명]';

-- 테이블
SELECT table_name FROM user_tables
MINUS
SELECT table_name FROM user_tables@hr_link;

SELECT table_name FROM user_tables@hr_link
MINUS
SELECT table_name FROM user_tables;

-- 인덱스
SELECT index_name FROM user_indexes
MINUS
SELECT index_name FROM user_indexes@hr_link;

SELECT index_name FROM user_indexes@hr_link
MINUS
SELECT index_name FROM user_indexes;

-- 제약
SELECT constraint_name FROM user_constraints
MINUS
SELECT constraint_name FROM user_constraints@hr_link;

SELECT constraint_name FROM user_constraints@hr_link
MINUS
SELECT constraint_name FROM user_constraints;

-- 시퀀스
SELECT sequence_name FROM user_sequences
MINUS
SELECT sequence_name FROM user_sequences@hr_link;

SELECT sequence_name FROM user_sequences@hr_link
MINUS
SELECT sequence_name FROM user_sequences;

-- 시너님
SELECT synonym_name FROM user_synonyms
MINUS
SELECT synonym_name FROM user_synonyms@hr_link;

SELECT synonym_name FROM user_synonyms@hr_link
MINUS
SELECT synonym_name FROM user_synonyms;

-- 뷰
SELECT view_name FROM user_views
MINUS
SELECT view_name FROM user_views@hr_link;

SELECT view_name FROM user_views@hr_link
MINUS
SELECT view_name FROM user_views;




-- 메타 데이터 --------------------------------------------

- AS-IS 12c : Export
expdp system/oracle \ SCHEMAS=hr \ DIRECTORY=datapump_dir \ DUMPFILE=hr_meta.dmp \ LOGFILE=hr_meta_exp.log \
      CONTENT=METADATA_ONLY \ EXCLUDE=TABLE,INDEX,CONSTRAINT,TRIGGER

- 12c  →  19c 전송
scp [HOME]/pump_ora12c/hr_meta.dmp \ [TO-BE OS 계정]@[TO-BE IP]:[HOME]/pump_ora19c/

- TO-BE 19c : Import
impdp system/oracle_4U \ DIRECTORY=datapump_dir \ DUMPFILE=hr_meta.dmp \ LOGFILE=hr_meta_imp.log \
      REMAP_SCHEMA=hr:hr2

-----------------------------------------------------------

   
</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>
