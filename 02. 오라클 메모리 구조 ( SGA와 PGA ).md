<pre>

SGA (System Global Area)  : 모든 서버 프로세스와 사용자가 공동으로 사용 = 공유 메모리
PGA (Program Global Area) : 개별 서버 프로세스가 독립적으로 사용 = 비공유 메모리 (정렬, 세션 변수 관리 등..)


● 1. SGA 내부 - Variable Size (가변 영역)

  - Shared Pool (공유 풀) : SQL Execution Plan, Data Dictionary 정보 공유
    ① Library Cache         : 사용자 SQL 문장, 실행 계획 저장 → ★ 똑같은 쿼리를 던질 때 재사용
    ② Data Dictionary Cache : 테이블 구조, 컬럼 정의, 권한 정보 같은 메타데이터를 저장합 ( = Row Cache )

  - Large Pool   : 공유 서버 환경이나 백업/복구 작업 등 대규모 메모리가 필요한 작업
  - Java Pool    : 자바 명령어를 실행
  - Streams Pool : 오라클 스트림즈(데이터 복제 등) 기능을 사용

● 2. ★ SGA 내부 - DB Buffers Cache : 디스크에서 읽은 데이터 블록을 메모리에 보관하는 메인 공간

● 3. SGA 내부 - Redo Log Buffers : 데이터의 모든 변경 사항을 로그 파일로 기록하기 전 잠깐 담아두는 버퍼 메모리




# RMAN으로 백업하기 
  
[oracle@ora19c ~]$ rman target / nocatalog
RMAN> backup database;

※ 백업하다가 모르고 Ctrl + C 를 눌러 취소해버렸을 때
  - 백업 상태 확인
    RMAN> list backup;
    RMAN> list backup summary;

  - 물리적 파일과 카탈로그 정보를 일치시키고 정리
    RMAN> crosscheck backup;
    RMAN> delete expired backup;

  - 이후 다시 백업
    RMAN> backup database;



# SGA와 그 구성요소 사이즈 확인하기
  
SQL> select sum(value) /1024/1024/1024 AS GB from v$sga;
        GB
----------
.999998212

SQL> select component, current_size/1024/1024 AS MB
       from v$sga_dynamic_components
      order by 2 desc;  -- save sga.sql
COMPONENT                              MB
------------------------------ ----------
DEFAULT buffer cache                  392
shared pool                           196
java pool                               4
large pool                              4
RECYCLE buffer cache                    0
ASM Buffer Cache                        0
DEFAULT 4K buffer cache                 0
DEFAULT 8K buffer cache                 0
DEFAULT 16K buffer cache                0
DEFAULT 32K buffer cache                0
Shared IO Pool                          0
Data Transfer Cache                     0
In-Memory Area                          0
In Memory RW Extension Area             0
In Memory RO Extension Area             0
KEEP buffer cache                       0
memoptimize buffer cache                0
unified pga pool                        0
streams pool                            0
DEFAULT 2K buffer cache                 0
  


# PGA 사이즈 확인
  
SQL> col name for a30
SQL> select name, value/1024/1024 as mb
       from v$pgastat
      where name in ('total PGA allocated', 'total PGA inuse', 'maximum PGA allocated');  -- save pga.sql
NAME                                   MB
------------------------------ ----------
total PGA inuse                220.816406    -- 지금 쓰는 용량
total PGA allocated            281.917969    -- 지금 확보된 PGA 사이즈
maximum PGA allocated          366.214844    -- 최대 용량 기록

-- total PGA inuse       : 현재 PGA 사용량
-- total PGA allocated   : 오라클이 OS에서 PGA로 확보 해둔 총량 (inuse + 대기중인 공간)
-- maximum PGA allocated : 인스턴스 시작 후 최대 확보했던 PGA 공간 




------------------------------------------------------------------------------------------------------



● SGA - Shared Pool : 파싱된 SQL, 실행 계획을 올려놓는 메모리

- Parsing
  ① 문법 검사 : SQL 문법 확인 ( syntax check )
  ② 의미 검사 : 쿼리의 테이블ㆍ컬럼이 존재하는 지, 권한이 있는 지 등을 검사 ( semantic check )


<img width="942" height="148" alt="image" src="https://github.com/user-attachments/assets/d24f29ea-c416-47df-b24a-b0de0749cac8" />


- SQL 최적화
  ① Query Transfomer : 사용자가 던진 SQL → 최적화하기 위해 쉬운 형태로 변환
  ② Plan Generator   : 후보군 실행계획 생성
  ③ Estimator        : 오퍼레이션 별 선택도ㆍ카디널리티ㆍ비용 계산  ( 실행 계획 전체의 총 비용 계산 )
    (오브젝트 통계, 시스템 통계)

- Row-Source Generator
  최적화 후 만들어진 실행계획 → 실행 가능한 코드/프로시저로 포매팅


★ 하드 파싱   : SQL 파싱 → SQL 최적화 → Row-Source 생성 → 실행
★ 소프트 파싱 : SQL 파싱 → 실행
  



# 하드파싱 구현

SET SERVEROUTPUT ON
 
DECLARE
   TYPE RC IS REF CURSOR;
   L_RC RC;
   L_DUMMY ALL_OBJECTS.OBJECT_NAME%TYPE;
   L_START NUMBER DEFAULT DBMS_UTILITY.GET_TIME;
 
BEGIN
  FOR i IN 1 .. 10000 LOOP
    OPEN L_RC FOR
      'SELECT OBJECT_NAME FROM ALL_OBJECTS WHERE OBJECT_ID = ' || i; -- 하드파싱 : 10000번을 돌려서 210초 걸림
      FETCH L_RC INTO L_DUMMY;
    CLOSE L_RC;
  END LOOP;
 
  DBMS_OUTPUT.PUT_LINE ( ROUND( (DBMS_UTILITY.GET_TIME - L_START)/100, 2) || 'SECONDS');
END;
/

</pre>
<br>
<br>
↓ 실행 시 CPU가 치솟기 시작함 (약 210초 걸림)<br>
<br>

![GIF](https://github.com/user-attachments/assets/d33649da-bb6a-429f-8f71-28f807b74bc2)
<br>
<br>
위에서 실행중인 하드파싱 SQL을 확인하는 방법<br>
<br>
![exec](https://github.com/user-attachments/assets/75aceb97-0de4-4926-9f02-df2fc6cda5a5)
<br>
<br>
<br>
<br>
<pre>
# 소프트 파싱 구현

SET SERVEROUTPUT ON

DECLARE
   TYPE RC IS REF CURSOR;
   L_RC RC;
   L_DUMMY ALL_OBJECTS.OBJECT_NAME%TYPE;
   L_START NUMBER DEFAULT DBMS_UTILITY.GET_TIME;

BEGIN
  FOR i IN 1 .. 10000 LOOP
    OPEN L_RC FOR
      'SELECT OBJECT_NAME FROM ALL_OBJECTS WHERE OBJECT_ID = :x' using i ; -- 소프트 파싱
      FETCH L_RC INTO L_DUMMY;
    CLOSE L_RC;
  END LOOP;

  DBMS_OUTPUT.PUT_LINE ( ROUND( (DBMS_UTILITY.GET_TIME - L_START)/100, 2) || 'SECONDS');
END;
/

0.5초 정도 걸림


  

------------------------------------------------------------------------------------------------------



● SGA - DB Buffer Cache : Data file에서 읽은 데이터 블럭을 올리는 메모리 영역
                          똑같은 데이터의 요청이 들어오면 메모리에서 바로 데이터를 보여줌


<img width="1005" height="478" alt="image" src="https://github.com/user-attachments/assets/03b0afc8-2578-4990-a3ff-a762b96926fe" />



Buffer Cache를 효율적으로 사용하기 위해 LRU 알고리즘 사용

Index Scan      : single block i/o 가 발생 → MRU로 올라감
Table Full Scan : multi block i/o 가 발생  → LRU로 올라감


※ 바로 윗 설명이 자꾸 헷갈려서 정리
  
LRU List
MRU (가장 최근 사용)                                               LRU (끝 부분)
  ㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁㅁ
  → → → → → → → → → → → → → → → → → → → → → → → → → → → → → → → → → → → →
  ↑ Index Scan시 올라감                                               ↑ Table Full Scan시 올라감 ( 한번 쓰고 버릴 확률 높음 )

MRU 쪽으로 올라가는 게 LRU 알고리즘  =>  자주 쓰는 쿼리 : LRU 알고리즘  =>  Index Scan은 MRU로 올리는 게 맞음 
  


# 자주 쓰는 쿼리 힌트 : cache
select /*+ cache */ sal from emp where ename = 'SCOTT';

CACHE   : Index Scan → MRU로 올라감, Table Full Scan → LRU로 올라감
NOCACHE : 둘 다 MRU로 올라감 ( Table Full Scan을 같은 쿼리로 자주할 때 유용할 듯 )




------------------------------------------------------------------------------------------------------


  
● SGA - Large Pool
  - RMAN 백업/복구 작업 시 Large Pool에서 버퍼가 할당됨 ( Shared Pool 사용 시 SQL 파싱 성능에 영향을 줌 )
  - Parallel Query
    ① 병렬 프로세스간 통신 버퍼 할당
    ② 메시지 큐 할당
    ③ 데이터 교환 영역 할당
  - Shared Server 모드에서는 PGA에 할당되던 UGA가 Large Pool에 저장됨 - OLTP
  - DBWR I/O slave
  - ASM 관련 작업
  - 일부 복구 프로세스

SQL> show parameter large_pool_size       -- 0 : AMM → SGA, PGA 크기 자동 조절
SQL> show parameter memory_target         -- 0 : 2000M 일 경우, 2000을 SGA와 PGA에게 자동 분배
SQL> show parameter sga_target            -- 1552M
SQL> show parameter pga_aggregate_target  -- 514M

  
-- Large Pool 명시
SQL> alter system set large_pool_size=256m scope=spfile;

-- 업무 중에 RMAN 백업에 비중이 있을 경우
-- 병렬쿼리 자주 사용  
-- Shared Pool 경합 자주 발생 

# 커널
    오라클이 메모리를 사용할 수 있도록 관리
    오라클의 AMM 기능을 쓰려고 memory_target을 2G로 잡음
    정작 커널이 관리하는 공유 메모리(df -h /dev/shm)는 1.8G → 커널이 메모리 할당 거부 에러

 ★ memory_max_target  <=  df -h /dev/shm  
  
-- AMM 수동으로 관리하여 해결
create spfile from pfile;  -- DB가 올라오지 않음 (파라미터 파일 생성)
alter system set memory_max_target=0       scope=spfile;
alter system set memory_target=0           scope=spfile;
alter system set sga_target=1536m          scope=spfile;
alter system set pga_aggregate_target=512m scope=spfile;



   
------------------------------------------------------------------------------------------------------



   
● SGA - Java Pool : Oracle JVM(자바 가상 머신)을 위한 전용 메모리 공간

다음과 같은 JAVA 코드 실행 가능
CREATE OR REPLACE AND RESOLVE JAVA SOURCE NAMED "HelloWorld" AS
public class HelloWorld
{
  public static String sayHello() { return "Hello Oracle"; }
}
/


   
------------------------------------------------------------------------------------------------------



● PGA : 정렬, 해쉬 조인 작업
   ORDER BY,  SORT MERGE JOIN,  CREATE INDEX,  데이터 분석 함수 등등...
   과도한 악성 SQL 수행 → PGA 영역이 꽉참 → 이후로 TEMP TableSpace 공간 사용 ( 성능 저하 )



# 악성 정렬 작업 실행

<img width="1530" height="671" alt="image" src="https://github.com/user-attachments/assets/9cfda492-b0d2-4e73-ba50-eb0dff12d17a" />


   

# 기본 Temp TableSpace 확인, Temp TableSpace 위치 확인

<img width="818" height="321" alt="image" src="https://github.com/user-attachments/assets/dc775a5c-aa13-494e-81f0-9e3464cc5a69" />


   

# 테스트를 위한 작은 temp 공간 만들기
   
create temporary tablespace temp2
 tempfile '[ORACLE_BASE]/oradata/[ORACLE_SID]/temp02.dbf' 
 size 2m reuse autoextend off;
 
alter user sys temporary tablespace temp2; 
alter user scott temporary tablespace temp2;

alter database default temporary tablespace temp2;

<img width="591" height="529" alt="image" src="https://github.com/user-attachments/assets/3087d341-a116-4949-a057-57d7d7e52929" />
   



# 위 문제 해결하기 (둘 중 하나 선택)

-- temp2 테이블 스페이스에 파일 추가
alter tablespace temp2
  add tempfile '[ORACLE_BASE]/oradata/[ORACLE_SID]/temp02b.dbf' size 200m;

-- 현재 사용 중인 temp파일 사이즈 증가 
alter database
  tempfile '[ORACLE_BASE]/oradata/[ORACLE_SID]/temp02.dbf' resize 200m;
   
alter user sys   temporary tablespace temp; 
alter user scott temporary tablespace temp;

alter database default temporary tablespace temp;

-- 테이블 스페이스 삭제 ( 명령 실행 안될 수도 있음 → 악성 SQL 세션 끄기 )
drop tablespace temp2 including contents and datafiles;



   
# 데이터 이행 + 야간 (= 사용자 거의 없음) → PGA 영역 크게 할당 받기

show parameter workarea_size_policy -- auto
alter session set workarea_size_policy = manual;

show parameter sort_area_size -- 65536
show parameter hash_area_size -- 131072

alter session set sort_area_size=1000000000;
alter session set hash_area_size=1000000000;


------------------------------------------------------------------------------------------------------


-- DBA 자동 스크립트 뼈대

SQL> @dba

==================================================
=              DBA MENU (Oracle 19c)             =
==================================================
1) SGA 크기 확인 (Total + Components)
2) PGA 크기 확인 (inuse / allocated / max)
3) datafile 위치
4) redo log file 위치
5) AMM  환경에서 조정되는 현재 메모리 컴포넌트 사이즈
0) 종료
==================================================

번호 입력 [0-5] :



   
</pre>
