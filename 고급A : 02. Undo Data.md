<pre>

● 1. Undo Data 
  
  - 수정되기 전 원래 데이터의 복사본
  - 데이터를 변경하는 모든 트랜잭션에 대해 캡처 ( 트랜잭션이 종료될 때 까지 보존 )
  - RollBack, 읽기 일관성, FlashBack, Failed Transaction Recovery

  - DML 작업을 수행하기 전 원본 데이터를 Undo TableSpace에 저장해두는 정보
  - 트랜잭션의 일관성과 복구를 보장하는 메커니즘



<img width="1024" height="683" alt="image" src="https://github.com/user-attachments/assets/3c08cd52-2db3-4cdc-b857-31ff1285a17c" />


update emp set sal = 0 where ename = 'SCOTT';
-- 변경 전 sal = 3000을 undo에 저장, 실제 블록에는 sal = 0 기록
-- DML, DDL 중인 모든 트랜잭션에 캡처  →  UNDO 발생   (DDL도 내부적으로 DML 수반)
-- 트랜잭션이 COMMIT / ROLLBACK 될 때 까지 보존   ( UNDO_RETENTION 파라미터로 더 보존 될 수 있음 (기본 900 초) )
-- commit을 해도 900초 동안 다른 트랜잭션이 덮어 쓰지 않음  →  flashback table, flashback query 가능
  

작업                           설명
롤백                           ROLLBACK 시 언두 데이터를 이용해 변경 전 상태로 되돌림
읽기 일관성 Query              다른 세션이 변경 중인 블록을 조회 시, 변경 전 값을 언두에서 읽어 일관된 결과 제공 (SCN)
Oracle Flashback Query         과거 특정 시점의 데이터 조회 (AS OF TIMESTAMP / SCN)
Oracle Flashback Transaction   특정 트랜잭션을 역으로 되돌리는 보상 트랜잭션 생성
Oracle Flashback Table         테이블 전체를 과거 시점으로 복원 (FLASHBACK TABLE ... TO TIMESTAMP)
실패한 트랜잭션 Recovery       인스턴스 장애 후 재시작 시, 커밋되지 않은 트랜잭션을 언두로 자동 정리




-- 언두 관리 방식 확인
SHOW PARAMETER undo_management;   -- AUTO 권장

-- 언두 테이블스페이스 확인
SHOW PARAMETER undo_tablespace;

-- 언두 보존 기간 (초)
SHOW PARAMETER undo_retention;



</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 2. ORA-01555 : snap shot too old

 - 쿼리 시작지점(SCN)의 Undo 데이터가 이미 덮어써져 사라졌을 때 발생하는 에러
 - 읽기 일관성을 위해 SCN의 데이터를 Undo로 재구성 해야함  VS   Undo 블록을 다른 트랜잭션이 재사용   →  SELECT 실패
 - SQLP 공부할 때 봤는데, 구현하기 어려운 에러라고 했었음


select tablespace_name, contents, retention  -- retention:NOGUARANTEE 시간 보장을 안함
  from dba_tablespaces
 where tablespace_name='UNDOTBS2'; 

alter tablespace undotbs2 retention guarantee;


-- UNDO RETENSION 파라미터 값을 늘려 UNDO 데이터 오래 유지
-- 쿼리 최적화 (수행 시간 단축), COMMIT 자주 X,
   ORDER BY 일부러 사용하여 TEMP Segment에 담아 두기, TABLE FULL SACN (우회)



</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 3. FlashBack 확실히 보장 ( Total Recall = FDA (Flashback Data Archive) ) 

  - 테이블의 모든 변경 이력을 별도 아카이브 공간에 자동 저장
  - 몇 년전 데이터도 조회할 수 있음 ( Undo만으로는 불가 )

  
-- flashback 권한
grant flashback archive administer to scott;

-- total recall을 위한 tablespace 
create tablespace fda_tbs datafile '/home/oracle/oradata/fda_tbs01.dbf' size 50m;

-- flashback data archive 생성
connect scott/tiger

create flashback archive fda1
tablespace fda_tbs
 retention 1 year;


-- emp 테이블에 fda 적용
alter table emp flashback archive fda1;

-- 삭제 (dml)
delete from emp;
commit; 

  
-- commit 전 데이터 확인
select *
  from emp as of timestamp to_timestamp('2026/03/12 11:20:00');

  
-- delete 전으로 flashback table
alter table emp enable row movement;
flashback table emp to timestamp to_timestamp('2026/03/12 11:20:00');

select * from emp;



- 원래 상태로 원복

alter table emp no flashback archive;
drop flashback archive fda1;

connect / as sysdba
drop tablespace fda_tbs including contents and datafiles;





※ 기타

UNDO 데이터 상태 3가지

- Active UNDO    : 현재 트랜잭션이 아직 커밋/롤백 안 된 상태. 진행 중인 작업에 반드시 필요.
- Unexpired UNDO : 트랜잭션은 끝났지만 UNDO_RETENTION 시간이 아직 안 지난 것. Flashback 쿼리 등에 활용 가능.
- Expired UNDO   : UNDO_RETENTION 시간도 지난 것. 재사용 가능한 상태.



alter system set undo_management='manual' scope=spfile;  -- 수동 관리
alter system set undo_management='auto'   scope=spfile;  -- 자동 관리



  
</pre>
