<pre>

업무 자동화
  
스케줄 기반 작업 (시간)
① 매월 말일에 일련의 월말 업무 수행
② 하루에 두 번 테이블 및 인덱스 통계 계산
③ 현재 인덱스 재구축 완료 시 다음 인덱스 재구축 : Job 체이닝

조건 기반 작업 (특정 상황)
④ DB 일일 백업
⑤ 부적합한 서버 액세스 시도 - 시간별 보고서 생성

이벤트 기반 작업 (특정 일 발생 시)
⑥ 메시지가 큐에 들어오는 즉시 Dequeue 프로시저 실행
⑦ 파일 시스템에 파일이 도착하는 즉시 일괄 처리 로드 시작

데이터 복제
⑧ Materialized View refresh를 통해 테이블 데이터 복제


유형         방법
시간 기반    DBMS_SCHEDULER로 정해진 시간에 실행
조건/체인    이전 Job 완료 후 자동 다음 Job 실행
이벤트 기반  파일 도착, 메시지 도착 시 즉시 실행
데이터 복제  Materialized View로 자동 갱신

</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 스케줄러 작업

create table emp01 ( hiredate date );  -- 테이블 생성

begin                                  -- 스케줄 생성
  dbms_scheduler.create_schedule
  ( schedule_name   => 'scott_insert_sch'
  , repeat_interval => 'freq=secondly; interval=5'
  );
end;
/

begin                                  -- 프로그램 생성
  dbms_scheduler.create_program
  ( program_name   => 'scott_insert_prog'
  , program_type   => 'PLSQL_BLOCK'
  , program_action => 'begin insert into emp01(hiredate) values (sysdate); end;'
  );
end;
/

begin                                  -- 작업 생성 ( 스케줄 + 프로그램 )
  dbms_scheduler.create_job 
  ( job_name      => 'scott_insert',       -- 작업명
  , program_name  => 'scott_insert_prog',  -- 프로그램
  , schedule_name => 'scott_insert_sch'    -- 스케줄
  );
end;
/

exec dbms_scheduler.enable('scott_insert_prog');  -- 작업, 프로그램 활성화
exec dbms_scheduler.enable('scott_insert');

select owner, program_name, enabled               -- 프로그램 활성화 확인
  from dba_scheduler_programs
 where owner='SCOTT' and program_name='SCOTT_INSERT_PROG';

select owner, job_name, enabled, state            -- 스케줄 활성화 확인
  from dba_scheduler_jobs
 where owner='SCOTT' and job_name='SCOTT_INSERT';

select * from emp01;                              -- 결과 확인 (insert로 현재시간 넣은 거 확인)

select *                                          -- 스케줄러 수행 내역 확인
	from dba_scheduler_job_run_details
 where owner = 'SCOTT';

select owner, job_name, next_run_date             -- 다음 실행 시간 확인
  from dba_scheduler_jobs
 where owner = 'SCOTT'
   and job_name = 'SCOTT_INSERT';  

exec dbms_scheduler.disable('scott_insert');            -- 작업 중지
exec dbms_scheduler.drop_job('scott_insert');           -- Job 삭제
exec dbms_scheduler.drop_schedule('scott_insert_sch');  -- 스케줄 삭제
exec dbms_scheduler.drop_program('scott_insert_prog');  -- 프로그램 삭제
  
</pre>
