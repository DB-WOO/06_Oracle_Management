<pre>

● Dynamic Performance View

  - 데이터 파일이 아닌 메모리(SGA)와 컨트롤 파일로부터 직접 정보를 읽어옴
  - 인스턴스가 가동 중일 때만 데이터가 존재
  - 세션 데이터, 대기 이벤트, SQL 실행 현황, 메모리 할당 상태 등 실시간 모니터링 및 튜닝에 필수


EX) v$sql  v$session  v$lock
  
SELECT sql_text, executions FROM v$sql
 WHERE cpu_time > 400000;

SELECT * FROM v$session
 WHERE machine = 'ABCDE5Z3' AND LOGON_TIME > SYSDATE - 1;

SELECT sid, ctime FROM v$lock
 WHERE block > 0;
  
</pre>
