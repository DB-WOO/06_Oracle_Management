<pre>

● 1. 외부 테이블 생성 - SQL*Loader 
  - 외부의 OS 파일에 저장된 데이터를 마치 일반 테이블처럼 SQL로 조회할 수 있게 해주는 읽기 전용 테이블
  - 데이터는 외부에 존재, 내부에는 메타 데이터(구조)만 존재 

  - DML 불가 + 인덱스 생성 불가
  - 오라클 DIRECTORY 오브젝트로 경로를 추상화
  - 병렬쿼리 가능 + 일반 테이블과 조인, GROUP BY 등 전체 문법 사용 가능

장점 : 데이터 적재 전 사전 검증
       운영 로그·감사 파일 분석
       DB 공간 절약
       DB 간 데이터 이동 (ORACLE_DATAPUMP) : Export  →  다른 DB : External Table 읽기  →  INSERT SELECT 
                                         ┗ 경량 마이그레이션 파이프라인 구성
       병렬 처리로 대용량 처리 성능 확보



-- 디렉토리 생성
mkdir -p [HOME]/scott_dir
chmod 755 [HOME]/scott_dir
ls -ld [HOME]/scott_dir

  
-- emp1.txt 텍스트 파일 생성
cat > [HOME]/scott_dir/emp1.txt <<'EOF'
7369,SMITH,CLERK,7902,1980-12-17,800,20
7499,ALLEN,SALESMAN,7698,1981-02-20,1600,30
7521,WARD,SALESMAN,7698,1981-02-22,1250,30
7566,JONES,MANAGER,7839,1981-04-02,2975,20
EOF



-- DB DIRECTORY 생성 ( sys )
CREATE OR REPLACE DIRECTORY scott_dir AS '[HOME]/scott_dir';
GRANT READ, WRITE ON DIRECTORY scott_dir TO scott;

SELECT directory_name, directory_path
  FROM dba_directories
 WHERE directory_name = 'SCOTT_DIR';




-- External Table 생성 ( scott )

CREATE TABLE emp1_ext
( empno    NUMBER(4)
, ename    VARCHAR2(10)
, job      VARCHAR2(9)
, mgr      NUMBER(4)
, hiredate DATE
, sal      NUMBER(7,2)
, deptno   NUMBER(2)  )
ORGANIZATION EXTERNAL
( TYPE ORACLE_LOADER
  DEFAULT DIRECTORY scott_dir
  ACCESS PARAMETERS
  ( RECORDS DELIMITED BY NEWLINE
    FIELDS TERMINATED BY ','
    MISSING FIELD VALUES ARE NULL
    ( empno
    , ename
    , job
    , mgr
    , hiredate CHAR(10) DATE_FORMAT DATE MASK "YYYY-MM-DD"
    , sal
    , deptno
    )
  )
  LOCATION ('emp1.txt')
)
REJECT LIMIT UNLIMITED;

-- 외부 테이블 가능 : view 생성, 병렬 쿼리, 조인
-- 외부 테이블 불가 : 트리거 생성, DML, 인덱스 생성

  
</pre>

<br>
<br>
<hr>
<br>
<br>

<pre>

● 2. 외부 테이블 생성 - Data Pump

SQL*Loader : 텍스트 파일, CSV 파일 등...
DATA PUMP  : 바이너리 파일 (.dmp)

구분        ORACLE_LOADER         ORACLE_DATAPUMP
파일 형식   텍스트 (CSV 등)        이진 (binary)
파일 → DB   가능 (Load)            가능 (Load)
DB → 파일   불가                   가능 (Unload)
속도        상대적으로 느림        빠름
용도        외부 텍스트 파일 조회  DB 간 데이터 이관


용어      방향          의미
Load      파일 → DB     외부 파일을 DB 안으로 적재
Unload    DB → 파일     DB 데이터를 외부 파일로 추출



DATA PUMP : 가능 / 불가 한 것들이 SQL*Loader랑 비슷
            파티셔닝도 가능
            DIRECTORY 객체 필수




-------- AS-IS : 12c --------------------------------------------------------------------------
  
-- 테스트 유저, 권한, 테이블, 데이터 만들었다고 가정 ( scott )

-- 디렉토리 생성 ( sys )
CREATE OR REPLACE DIRECTORY pump_ora12c AS '[HOME]/pump_ora12c';
GRANT READ, WRITE ON DIRECTORY pump_ora12c TO scott;

  
-- 외부로 보낼 테이블 생성 ( scott )

CREATE TABLE emp_ext_export
ORGANIZATION EXTERNAL
( TYPE ORACLE_DATAPUMP
  DEFAULT DIRECTORY pump_ora12c
  LOCATION ('emp.dmp') )
AS SELECT * FROM emp;

select count(*) from emp_ext_export;

-- TO-BE 전송
scp [HOME]/pump_ora12c/emp.dmp [TO-BE OS 계정]@[TO-BE IP]:[TO-BE 디렉토리]
  
-----------------------------------------------------------------------------------------------

-------- TO-BE : 19c --------------------------------------------------------------------------

-- 디렉토리 생성 ( sys )
CREATE OR REPLACE DIRECTORY pump_ora19c AS '[HOME]/pump_ora19c';
GRANT READ, WRITE ON DIRECTORY pump_ora19c TO scott;

-- 외부 테이블 생성 ( scott )
CREATE TABLE emp_ext
( empno    NUMBER(4)
, ename    VARCHAR2(10)
, job      VARCHAR2(9)
, mgr      NUMBER(4)
, hiredate DATE
, sal      NUMBER(7,2)
, comm     NUMBER(7,2)
, deptno   NUMBER(2)  )
ORGANIZATION EXTERNAL
( TYPE ORACLE_DATAPUMP
	DEFAULT DIRECTORY pump_ora19c
	LOCATION ('emp.dmp') )
REJECT LIMIT UNLIMITED;

SELECT * FROM emp_ext;

-----------------------------------------------------------------------------------------------

</pre>

